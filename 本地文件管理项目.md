# 启禾文件管理

一个基于 **Electron + TypeScript + SolidJS + Tailwind CSS** 的跨平台桌面应用（Windows / Linux），用于管理电商产品图包与证书文件。

---

## 一、核心定位

**"产品集驱动的文件工厂"**

- 以 **产品集（Product Set）** 为最高组织单元
- 以 **SKU** 为最小操作原子
- 工作区自包含：文件、配置、元数据在同一个文件夹内（缩略图缓存 v2.1.0 起位于 userData，不再污染工作区）
- 应用内完成"选产品 → 建结构 → 放文件"全流程，无需打开 Windows 资源管理器
- v2.4.0 起支持：文件移动到其他文件夹/产品集、目录拖入导入、批量重命名、应用内更新检查
- **v2.5.x（2026-08）**：插件宿主体系落地 + 全业务预填 + 云桥 M3 + AI 助手 + 发票识别 + 卡片化 + 批量 AI 识别待确认区 + MoneyInput + LAN 白名单收口 + 供应商子文件夹 + 客户/供应商文件区按钮导入 + 登录态持久

---

## 二、工作区（Workspace）

### 2.1 工作区概念

| 特性 | 说明 |
|------|------|
| **自包含** | 一个文件夹 = 所有文件 + 配置 + 元数据，不依赖外部系统 |
| **可迁移** | 复制整个工作区到 U 盘、NAS、另一台电脑，打开即用 |
| **多工作区** | 可同时维护多个工作区，应用内一键切换 |
| **云盘无关** | 可放在本地、移动硬盘、OneDrive、坚果云等任意位置，应用只读本地文件 |
| **默认工作区** | 启动时自动恢复最近打开的工作区；首次使用自动创建 `用户主目录/启禾文件管理` 并打开，开箱即用 |

### 2.2 工作区目录结构

```
MyWorkspace/                    ← 工作区根目录（用户指定）
├── .qihefilemanager/           ← 应用数据（隐藏文件夹）
│   ├── config.json             ← 工作区配置：命名模板、子文件夹类型、客户子文件夹
│   ├── metadata.json           ← 文件元数据：标签、证书到期日、备注
│   ├── customers.json          ← 客户档案（v2.4.7：别名/国家/联系方式/来源/标签/备注/关联产品集/erp_ext 预留）
│   ├── invoices.json           ← 发票台账（v2.4.7：号码查重主键 + 状态流转 + 待办日期）
│   ├── inbound.json            ← 入库单（v2.4.7：单据编号查重主键 + 6 字段）
│   ├── exchange_state.json     ← 交换区已处理投递 id 簿记（v2.4.7，滚动 500 条）
│   └── trash/                  ← 回收站（v2.5.x）
│
├── 产品集/                      ← 产品集 → SKU 层级（应用管理）
│   └── 夏季T恤系列/
│       ├── SKU001_白色圆领/
│       │   ├── 图包/
│       │   │   ├── 主图/
│       │   │   ├── 详情页/
│       │   │   ├── 白底图/
│       │   │   └── 素材/
│       │   └── 证书/
│       │       ├── 3C/
│       │       ├── 质检/
│       │       └── 专利/
│       └── SKU002_黑色V领/
│           └── ...
│
├── 客户/                        ← 客户维度（v2.4.7：目录扫描为实，JSON 为档案）
│   └── 张三/
│       ├── 报价/
│       ├── 合同/
│       ├── 沟通/
│       └── 其他/
│
├── 供应商/                      ← 供应商维度（v2.5.x）
│   └── 供应商A/
│       ├── 报价/
│       └── 其他/
│
├── 报价/                        ← 报价单文件归档（v2.5.x，按报价日期年份）
│   └── 2026/
│
├── 发票/                        ← 发票文件归档（v2.4.7，按开票日期年份）
│   └── 2026/
├── 入库/                        ← 入库单文件归档（v2.4.7，按入库日期年份）
│   └── 2026/
├── 交换区/                      ← 投递区（v2.4.7：文件 + 描述 JSON；已处理/ 放回执与已消费描述）
│   └── 已处理/
│
├── 图包/                        ← 图包文件汇总区（与 产品集/ 下同步生成）
├── 证书/                        ← 证书文件汇总区（与 产品集/ 下同步生成）
├── 文档/                        ← 通用文档区（v2.5.x）
│
└── 导出/                        ← 临时导出区域
```

> 当前实现中，文件统一存放在 `产品集/<产品集>/<SKU>/图包/` 与 `产品集/<产品集>/<SKU>/证书/` 下，`图包/` 与 `证书/` 根目录保留为后续导出/汇总功能预留。v2.4.7 起客户/发票/入库/交换区四个根目录为常驻区域（旧工作区打开自动补齐），`发票/`、`入库/` 区只经台账归档与交换区受控写入。

---

## 三、命名自动化

### 3.1 命名模板引擎

每个工作区独立配置，存储在 `.qihefilemanager/config.json`：

| 元素 | 默认规则 | 可配置 |
|------|---------|--------|
| 产品集文件夹 | `{产品集名称}` | 前缀/后缀模板 |
| SKU 文件夹 | `{SKU}_{产品简称}` | 分隔符、字段顺序 |
| 图包子文件夹 | `主图` / `详情页` / `白底图` / `素材` | 可增删自定义 |
| 证书子文件夹 | `3C` / `质检` / `专利` | 可增删自定义 |
| 文件命名冲突 | 自动加序号：`主图_1.jpg` | 序号模板 `{n}` |

### 3.2 文件导入命名示例

拖拽 `banner.jpg` 到 `SKU001` 的 `主图` 文件夹，若产品简称为"白色圆领"：

```
产品集_夏季T恤系列/
└── SKU001_白色圆领/
    └── 图包/
        └── 主图/
            └── SKU001_白色圆领_banner.jpg
```

若文件名冲突，自动变为：

```
SKU001_白色圆领_banner_1.jpg
```

### 3.3 非法字符处理

产品名、SKU、文件名中的 `\ / : * ? " < > |` 等字符，应用自动替换为 `_`，保证文件系统合法。

---

## 四、应用内操作流程

### 4.1 新建工作区

1. 点击顶部栏"选择工作区"
2. 选择"新建工作区"或"打开工作区"
3. 选择目标文件夹
4. 应用自动创建 `.qihefilemanager/` 与标准目录结构

### 4.2 新建产品集

1. 进入"产品集"页面
2. 点击"新建产品集"
3. 输入产品集名称
4. 应用生成该产品集下的所有 SKU 占位结构

> 在"新建产品集"弹窗中可一次性添加多个 SKU，点击"确认创建"后会批量创建产品集及其下的所有 SKU。

### 4.3 新建 SKU

1. 进入某个产品集详情
2. 点击"新建 SKU"
3. 输入 SKU 编号、产品名称、产品简称
4. 应用同时在图包侧与证书侧创建标准子文件夹

### 4.4 新建子文件夹类型

1. 进入 SKU 的图包或证书浏览视图
2. 点击"新建图包类型"或"新建证书类型"
3. 输入名称（如 `FDA认证`）
4. 应用创建子文件夹并更新配置

### 4.5 批量导入产品集与 SKU（XLSX）

1. 进入"产品集"页面
2. 点击"XLSX 模板"下载带样式和示例的 Excel 模板
3. 按模板填写产品集名称、SKU 编号、产品名称、产品简称
4. 点击"XLSX 导入"，选择填写好的 `.xlsx` 文件
5. 应用自动按产品集分组，批量创建产品集和 SKU

---

## 五、拖拽导入

### 5.1 拖拽目标

| 目标 | 行为 |
|------|------|
| **全局拖拽区** | 文件拖到应用窗口任意位置，弹出"选择导入目标"对话框 |
| **SKU 卡片** | 拖到产品集页面的 SKU 卡片上，弹出选择器：图包/证书 → 子文件夹 |
| **文件浏览区** | 直接拖入当前子文件夹，复制到该位置并按命名模板重命名 |

### 5.2 拖拽后处理

1. 文件复制到目标路径
2. 按命名模板重命名
3. 同名文件自动加序号
4. 图片自动生成缩略图（PDF 以预览查看），缓存于用户数据目录（`~/.config/启禾文件管理/thumbs`），旧工作区缓存自动迁移复用
5. 写入 `metadata.json`：添加时间、标签、证书类型、到期日

---

## 六、前端视图

```
应用窗口
├── 顶部栏
│   ├── 工作区选择器（新建/打开/切换/最近工作区）
│   └── 全局搜索框（快捷键 Enter 触发）
│
├── 左侧导航
│   ├── 仪表盘
│   ├── 产品集
│   ├── 图包库
│   ├── 证书库
│   ├── 客户（v2.4.7）
│   ├── 供应商（v2.5.x）
│   ├── 报价（v2.5.x）
│   ├── 发票（v2.4.7）
│   ├── 入库单（v2.4.7）
│   ├── 搜索
│   ├── 设置
│   └── 回收站
│
├── 主内容区
│   ├── 仪表盘：统计卡片、最近文件、到期提醒、发票待办
│   ├── 产品集：卡片网格 / 产品集详情（概览条 + 关联客户/供应商只读区块）/ SKU 卡片
│   ├── 图包库：所有图片聚合浏览
│   ├── 证书库：所有证书聚合浏览
│   ├── 客户：卡片网格 / 客户详情（档案卡 + 子文件夹文件区 + 关联产品集）/ erp_ext 只读区
│   ├── 供应商：卡片网格 / 供应商详情（档案卡 + 子文件夹文件区 + 关联产品集）
│   ├── 报价：台账 Tab（表格 + 筛选 + 搜索 + 新建/编辑 + 行内状态流转 + 导出 + 合计行）/ 文档文件夹拖拽复制
│   ├── 发票：卡片网格 / 发票详情（档案卡 + 归档文件区 + 批量识别待确认区）/ 台账 Tab（卡片化 + 批量工具条 + 筛选增强）
│   ├── 入库单：卡片网格 / 入库单详情（档案卡 + 归档文件区）/ 台账 Tab（卡片化 + 批量工具条 + 筛选增强）
│   ├── 搜索：按关键词跨产品集/客户/供应商/SKU/文件搜索
│   ├── 设置：命名模板、子文件夹类型、客户子文件夹配置、供应商子文件夹配置
│   └── 回收站：文件/子文件夹/产品集/客户/供应商统一恢复与彻底删除
│
└── 全局拖拽遮罩
    └── 文件拖入时显示"释放以导入文件"
```

---

## 七、后端架构

### 7.1 技术栈

| 层级 | 选型 | 说明 |
|------|------|------|
| 跨平台壳 | **Electron** | 窗口、Chromium 渲染、主进程 IPC、系统对话框、托盘 |
| 主进程 | **TypeScript** | 业务层与 Electron API 解耦（`src/main/core/`，可 node 直测） |
| 前端框架 | **SolidJS** | 响应式 UI，适合文件列表高频更新 |
| 样式 | **Tailwind CSS** | 快速构建桌面级界面 |
| 缩略图 | **sharp** | 图片缩略图生成（限并发队列）；PDF 不做缩略图（预览查看） |
| 数据持久化 | **JSON 文件** | `config.json` + `metadata.json` |
| 文件预览 | **qihebox:// 自定义协议** | 流式 + Range，大图/PDF/视频不整读内存 |
| 剪贴板 | Win CF_HDROP / Linux text/uri-list | 复制文件到微信/钉钉直接粘贴 |

### 7.2 核心模块

| 模块 | 文件 | 职责 |
|------|------|------|
| 工作区管理 | `core/workspace.ts` | 创建/打开/切换工作区、最近工作区列表 |
| 文件操作 | `core/files.ts` | 文件列表、导入、删除、子文件夹创建（v2.4.7 支持 scope='customer'） |
| 元数据 | `core/metadata.ts` | 读取/写入 `metadata.json`（v2.4.7 key 泛化：客户/发票/入库/交换区文件可用） |
| 命名 | `core/naming.ts` | 命名模板、非法字符处理、冲突后缀 |
| 搜索 | `core/search.ts` | 跨产品集/SKU/文件搜索（v2.4.7 加客户实体 + 三区文件） |
| 仪表盘 | `core/dashboard.ts` | 统计、最近文件、到期证书检查、发票待办（v2.4.7） |
| 客户 | `core/clients.ts` | 客户目录 × customers.json 档案合并（v2.4.7） |
| 供应商 | `core/suppliers.ts` | 供应商目录 × 档案合并（v2.5.x） |
| 报价 | `core/quotes.ts` | 报价单台账 + 状态流转 + 归档（v2.5.x） |
| 发票台账 | `core/invoices.ts` | 查重/状态流转/待办窗口/归档/导出/标签引用源（v2.4.7）；卡片化 + 批量 AI 识别待确认区（v2.5.5/2.5.6） |
| 入库单 | `core/inbound.ts` | 单据编号查重 + 归档（v2.4.7）；卡片化 + 批量工具条（v2.5.5） |
| 批量识别 | `core/batchIdentify.ts` | 批量 AI 识别编排（v2.5.5）：选文件 → 插件识别 → 待确认区 → 登记落盘 |
| 交换区 | `core/exchange.ts` | 投递归集 + 回执 + 幂等簿记 + sweep/watch（v2.4.7） |
| XLSX | `core/xlsx.ts` | 生成 XLSX 模板、解析批量导入、通用 exportRows（发票导出） |
| 缩略图 | `thumbnail.ts` | sharp 生成图片缩略图，userData `thumbs/` 哈希路径 |
| 扫描缓存 | `core/scanCache.ts` | 目录树 mtime 签名，消除重复递归统计 |

### 7.3 数据流

```
用户操作（前端）
    ↓
window.qihebox API（preload contextBridge 白名单）
    ↓
IPC（ipcMain.handle，ApiResult<T> 包装）
    ↓
core 业务层（BoxService 及下属服务，纯 TS 不依赖 electron）
    ↓
文件系统操作 / JSON 读写
    ↓
返回 ApiResult<T> 给前端
    ↓
前端更新界面
```

---

## 八、数据持久化

### 8.1 config.json

```json
{
  "name": "Workspace",
  "naming_template": {
    "product_set_prefix": "",
    "product_set_suffix": "",
    "sku_separator": "_",
    "sku_fields": ["sku_code", "product_short_name"],
    "conflict_suffix": "_{n}"
  },
  "image_subfolders": ["主图", "详情页", "白底图", "素材"],
  "cert_subfolders": ["3C", "质检", "专利"]
}
```

### 8.2 metadata.json

```json
{
  "files": {
    "夏季T恤系列/SKU001_白色圆领/3C认证.pdf": {
      "cert_type": "3C",
      "expiry_date": "2028-06-01",
      "tags": ["重要"],
      "notes": "已上传平台",
      "added_at": "2026-06-24T10:00:00+08:00"
    }
  }
}
```

### 8.3 最近工作区

存储在用户主目录下的 `.qihefilemanager_recent.json`：

```json
[
  "C:/Users/.../工作区A",
  "D:/电商资料/2026新品"
]
```

---

## 九、证书到期提醒

- 证书视图可编辑到期日
- 仪表盘显示"30 天内到期"的证书列表
- 到期证书按到期日升序排列

---

## 十、开发与构建

### 10.1 环境要求

- Node.js 20+（建议 22+）
- npm 10+
- 打包安装包时 electron-builder 自动处理（Linux 需要基础构建工具）

### 10.2 常用命令

```bash
# 开发模式（热更新）
npm run dev

# 构建 Linux 包（AppImage + deb）
npm run build:linux

# 构建 Windows 安装包（NSIS，建议在 Windows/CI 构建）
npm run build:win
```

### 10.3 产物

- Linux：`release/启禾文件管理-*.AppImage` / `*.deb`
- Windows：`release/启禾文件管理 Setup-*.exe`

### 10.4 图标生成

应用图标源文件为 `build/logo.svg`。构建前如需更新图标，运行：

```bash
python scripts/render_logo.py
```

该脚本会生成：
- `build/appicon.png`（512×512，用于 electron-builder 打包应用图标（Windows/Linux））
- `build/trayicon.png`（64×64，用于系统托盘）
- `build/trayicon.ico`（系统托盘图标容器）

> 依赖：`Pillow`。首次运行前请先安装：`pip install Pillow`。

---

## 十一、用户典型工作流

```
上午：运营收到 5 个新品需求
    ↓
打开 启禾文件管理 → 选择工作区
    ↓
点击"新建产品集" → 输入"夏季T恤系列"
    ↓
进入产品集 → 新建 SKU：SKU001 / 白色圆领T恤 / 白色圆领
    ↓
设计师把修好的主图从桌面拖到 SKU001 卡片上
    ↓
选择"图包" → "主图" → 文件自动复制并重命名
    ↓
3C 认证 PDF 拖到证书区域
    ↓
选择"证书" → "3C" → 文件自动归档，并记录到期日
    ↓
财务查看仪表盘 → 看到最近文件与到期提醒
    ↓
关闭应用，整个工作区文件夹可打包带走或同步到云盘
```

---

## 十二、一句话总结

**启禾文件管理 是一个自包含的产品集驱动文件工厂：你导入产品集，它自动建好所有文件夹；你拖拽文件，它自动分类命名；你把整个工作区文件夹复制到任何电脑上，打开即用，无需配置，无需联网。**
