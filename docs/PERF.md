
## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 121ms
- **dashboard 缓存命中(连调2次)**: 198ms
- **搜索(2000文件)**: 137ms（命中 200）
- **产品集列表(200)**: 19ms
- **导入100文件(含缩略图)**: 270ms（100 张生成缩略图）
- **进程内存(RSS)**: 125MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 133ms
- **dashboard 缓存命中(连调2次)**: 203ms
- **搜索(2000文件)**: 133ms（命中 200）
- **产品集列表(200)**: 22ms
- **导入100文件(含缩略图)**: 297ms（100 张生成缩略图）
- **进程内存(RSS)**: 121MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 130ms
- **dashboard 缓存命中(连调2次)**: 222ms
- **搜索(2000文件)**: 146ms（命中 200）
- **产品集列表(200)**: 18ms
- **导入100文件(含缩略图)**: 313ms（100 张生成缩略图）
- **进程内存(RSS)**: 125MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 132ms
- **dashboard 缓存命中(连调2次)**: 221ms
- **搜索(2000文件)**: 146ms（命中 200）
- **产品集列表(200)**: 20ms
- **导入100文件(含缩略图)**: 278ms（100 张生成缩略图）
- **进程内存(RSS)**: 121MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 143ms
- **dashboard 缓存命中(连调2次)**: 210ms
- **搜索(2000文件)**: 140ms（命中 200）
- **产品集列表(200)**: 19ms
- **导入100文件(含缩略图)**: 277ms（100 张生成缩略图）
- **进程内存(RSS)**: 127MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 136ms
- **dashboard 缓存命中(连调2次)**: 226ms
- **搜索(2000文件)**: 145ms（命中 200）
- **产品集列表(200)**: 19ms
- **导入100文件(含缩略图)**: 276ms（100 张生成缩略图）
- **进程内存(RSS)**: 126MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 122ms
- **dashboard 缓存命中(连调2次)**: 203ms
- **搜索(2000文件)**: 132ms（命中 200）
- **产品集列表(200)**: 21ms
- **导入100文件(含缩略图)**: 261ms（100 张生成缩略图）
- **进程内存(RSS)**: 118MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 135ms
- **dashboard 缓存命中(连调2次)**: 203ms
- **搜索(2000文件)**: 131ms（命中 200）
- **产品集列表(200)**: 18ms
- **导入100文件(含缩略图)**: 251ms（100 张生成缩略图）
- **进程内存(RSS)**: 116MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 127ms
- **dashboard 缓存命中(连调2次)**: 212ms
- **搜索(2000文件)**: 131ms（命中 200）
- **产品集列表(200)**: 17ms
- **导入100文件(含缩略图)**: 251ms（100 张生成缩略图）
- **进程内存(RSS)**: 100MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 139ms
- **dashboard 缓存命中(连调2次)**: 197ms
- **搜索(2000文件)**: 128ms（命中 200）
- **产品集列表(200)**: 22ms
- **导入100文件(含缩略图)**: 259ms（100 张生成缩略图）
- **进程内存(RSS)**: 101MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 122ms
- **dashboard 缓存命中(连调2次)**: 196ms
- **搜索(2000文件)**: 150ms（命中 200）
- **产品集列表(200)**: 18ms
- **导入100文件(含缩略图)**: 252ms（100 张生成缩略图）
- **进程内存(RSS)**: 115MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 118ms
- **dashboard 缓存命中(连调2次)**: 174ms
- **搜索(2000文件)**: 117ms（命中 200）
- **产品集列表(200)**: 19ms
- **导入100文件(含缩略图)**: 237ms（100 张生成缩略图）
- **进程内存(RSS)**: 116MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 123ms
- **dashboard 缓存命中(连调2次)**: 192ms
- **搜索(2000文件)**: 115ms（命中 200）
- **产品集列表(200)**: 16ms
- **导入100文件(含缩略图)**: 227ms（100 张生成缩略图）
- **进程内存(RSS)**: 123MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 120ms
- **dashboard 缓存命中(连调2次)**: 209ms
- **搜索(2000文件)**: 132ms（命中 200）
- **产品集列表(200)**: 18ms
- **导入100文件(含缩略图)**: 253ms（100 张生成缩略图）
- **进程内存(RSS)**: 122MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 134ms
- **dashboard 缓存命中(连调2次)**: 232ms
- **搜索(2000文件)**: 136ms（命中 200）
- **产品集列表(200)**: 20ms
- **导入100文件(含缩略图)**: 236ms（100 张生成缩略图）
- **进程内存(RSS)**: 120MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 121ms
- **dashboard 缓存命中(连调2次)**: 213ms
- **搜索(2000文件)**: 126ms（命中 200）
- **产品集列表(200)**: 17ms
- **导入100文件(含缩略图)**: 229ms（100 张生成缩略图）
- **进程内存(RSS)**: 119MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 131ms
- **dashboard 缓存命中(连调2次)**: 214ms
- **搜索(2000文件)**: 136ms（命中 200）
- **产品集列表(200)**: 16ms
- **导入100文件(含缩略图)**: 237ms（100 张生成缩略图）
- **进程内存(RSS)**: 120MB

## 2026-08-08（渲染层/UI，Deepin，v2.1.0 虚拟滚动）
- 场景：1 万张图目录（产品集/基准测试/图包/主图 × 10000），Images 页虚拟滚动
- **DOM 节点数恒定**: 36~54 个卡片（视口 ± overscan，滚动全程不增长；旧版滚到底挂 1 万节点）
- **渲染进程 RSS**: 空载 156MB → 首屏 191MB → 滚动峰值 197MB → 回顶回落 194MB（增量 ≤41MB）
- **总内存基线**: ~700MB（Electron 43 + Deepin 软件渲染 GPU ~190MB，Chromium 固有开销）
- 缩略图：36/36 真实加载（qihebox://thumb/，userData 缓存，LRU 命中免 IPC）

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 124ms
- **dashboard 缓存命中(连调2次)**: 221ms
- **搜索(2000文件)**: 130ms（命中 200）
- **产品集列表(200)**: 17ms
- **导入100文件(含缩略图)**: 242ms（100 张生成缩略图）
- **进程内存(RSS)**: 123MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 122ms
- **dashboard 缓存命中(连调2次)**: 200ms
- **搜索(2000文件)**: 129ms（命中 200）
- **产品集列表(200)**: 18ms
- **导入100文件(含缩略图)**: 254ms（100 张生成缩略图）
- **进程内存(RSS)**: 122MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 124ms
- **dashboard 缓存命中(连调2次)**: 197ms
- **搜索(2000文件)**: 131ms（命中 200）
- **产品集列表(200)**: 17ms
- **导入100文件(含缩略图)**: 254ms（100 张生成缩略图）
- **进程内存(RSS)**: 122MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 122ms
- **dashboard 缓存命中(连调2次)**: 203ms
- **搜索(2000文件)**: 131ms（命中 200）
- **产品集列表(200)**: 18ms
- **导入100文件(含缩略图)**: 239ms（100 张生成缩略图）
- **进程内存(RSS)**: 126MB

## 2026-08-08（最终版 v2.1.0，Deepin，移除 PDF 缩略图后复测）
- 场景：1 万张图目录，Images 页虚拟滚动（app.getAppMetrics 各进程 workingSetSize）
- 空载：总 702MB = 主 282 + 渲染 158 + GPU 184 + 其他(zygote/utility) 78
- Images 首屏：总 773MB（渲染 191）；滚动到底 785MB（渲染 193）；回顶 786MB（渲染 194，回落）
- 峰值（滚动半程）：总 805MB（主 341 + 渲染 197）；DOM 节点恒定 36~54
- 结论：万图场景渲染进程增量 ≤39MB 且回落；~540MB 为 Electron/Chromium + Deepin 软件渲染 GPU 的平台基线

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 121ms
- **dashboard 缓存命中(连调2次)**: 202ms
- **搜索(2000文件)**: 132ms（命中 200）
- **产品集列表(200)**: 17ms
- **导入100文件(含缩略图)**: 234ms（100 张生成缩略图）
- **进程内存(RSS)**: 121MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 106ms
- **dashboard 缓存命中(连调2次)**: 202ms
- **搜索(2000文件)**: 118ms（命中 200）
- **产品集列表(200)**: 14ms
- **导入100文件(含缩略图)**: 211ms（100 张生成缩略图）
- **进程内存(RSS)**: 119MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 120ms
- **dashboard 缓存命中(连调2次)**: 184ms
- **搜索(2000文件)**: 120ms（命中 200）
- **产品集列表(200)**: 15ms
- **导入100文件(含缩略图)**: 214ms（100 张生成缩略图）
- **进程内存(RSS)**: 123MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 111ms
- **dashboard 缓存命中(连调2次)**: 184ms
- **搜索(2000文件)**: 125ms（命中 200）
- **产品集列表(200)**: 15ms
- **导入100文件(含缩略图)**: 207ms（100 张生成缩略图）
- **进程内存(RSS)**: 121MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 102ms
- **dashboard 缓存命中(连调2次)**: 178ms
- **搜索(2000文件)**: 111ms（命中 200）
- **产品集列表(200)**: 14ms
- **导入100文件(含缩略图)**: 220ms（100 张生成缩略图）
- **进程内存(RSS)**: 125MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 131ms
- **dashboard 缓存命中(连调2次)**: 211ms
- **搜索(2000文件)**: 136ms（命中 200）
- **产品集列表(200)**: 16ms
- **导入100文件(含缩略图)**: 226ms（100 张生成缩略图）
- **进程内存(RSS)**: 115MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 118ms
- **dashboard 缓存命中(连调2次)**: 203ms
- **搜索(2000文件)**: 137ms（命中 200）
- **产品集列表(200)**: 22ms
- **导入100文件(含缩略图)**: 253ms（100 张生成缩略图）
- **进程内存(RSS)**: 117MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 124ms
- **dashboard 缓存命中(连调2次)**: 212ms
- **搜索(2000文件)**: 136ms（命中 200）
- **产品集列表(200)**: 17ms
- **导入100文件(含缩略图)**: 248ms（100 张生成缩略图）
- **进程内存(RSS)**: 124MB

## 2026-08-08（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 123ms
- **dashboard 缓存命中(连调2次)**: 215ms
- **搜索(2000文件)**: 151ms（命中 200）
- **产品集列表(200)**: 18ms
- **导入100文件(含缩略图)**: 238ms（100 张生成缩略图）
- **进程内存(RSS)**: 123MB

## 2026-08-10（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 145ms
- **dashboard 缓存命中(连调2次)**: 207ms
- **搜索(2000文件)**: 152ms（命中 200）
- **产品集列表(200)**: 17ms
- **导入100文件**: 96ms（缩略图批量生成 5ms）
- **进程内存(RSS)**: 126MB

## 2026-08-11（v2.4.5 运行时降版：Electron 43 → 31.7.7，Deepin，smaps_rollup 进程树实测）
- 方法：`scripts/measure-memory.mjs`（Playwright 直起 electron，生产同款启动参数，XDG 隔离空工作区）；同机对照为正在运行的 prod v2.4.3（Electron 43，真实工作区）
- **醒着（窗口打开，空闲）**: RSS 合计 **487MB**（主 260 / utility 71 / 渲染 155）；PSS 261MB；独占 166MB；swap 0
  - vs E43 空载基线 580MB：**-16%**；同机 prod（508MB，真实数据 7h）：-4%
- **关窗倒计时中**: 同上（渲染仍存活，30s 销毁前）
- **托盘常驻（30s 休眠销毁后）**: RSS 合计 **310MB**（主 238 / utility 71，渲染归零）；PSS 184MB；独占 131MB
  - vs E43 休眠基线 408MB：**-24%**
- **T2（NetworkServiceInProcess）实测不生效**：直传启动参数下 network.mojom.NetworkService 仍独立成 utility 进程（RSS ~70MB），E31 不尊重该 feature → 按预案撤销，未固化
- **pdfjs 5.6 → 4.8.69**：`Promise.try`（Chrome 128+）在 Chromium 126 不存在致 PDF 预览白屏；4.9 起引入该调用，回退 4.8.69 后 PDF e2e 转绿
- 测试：141 单测 + 21 e2e 全绿（E31 + pdfjs 4.8.69）；打包 AppImage 97MB / deb 98MB / Win Setup 96MB
- 遗留：Windows 对照数、万图场景抽查（渲染层虚拟滚动机制未变）待实机/CI

## 2026-08-11（v2.4.6 内存/性能优化轮，Deepin，同口径实测）
- 方法同 v2.4.5（`scripts/measure-memory.mjs`，PROD_ARGS 已同步 v2.4.6 参数）
- **醒着（窗口打开，空闲 8s）**: RSS 合计 **464MB**（主 239 / utility 70 / 渲染 153）；PSS 249；独占 157；swap 0 —— vs v2.4.5 **-23MB**
- **托盘常驻（30s 休眠销毁后）**: RSS 合计 **295MB**（主 224 / utility 70，渲染归零）；PSS 178；独占 126 —— vs v2.4.5 **-15MB**，达成 ≤300MB 目标
- 收益构成：exceljs 懒加载（-24MB RSS 量级，与 sharp 同款动态 import）；删 `--max-semi-space-size=16`（V8 11.3 起 semi-space 自适应，显式 16 反抬到 ~48MB/isolate）；堆上限 1024→768；indexCache 快照配额 4096→512（大工作区生效，空载测量体现不出）；渲染层 `v8CacheOptions: 'code'`
- 回退记录：`--in-process-gpu` 按调研结论删除后实测 GPU 进程仍独立创建（~149MB），托盘 RSS 反升 +50MB（360），已恢复——`--disable-gpu` 在 E31/Deepin 不阻止 GPU 进程创建
- 渲染层配套：图片预览 2048px 降采样副本（解码位图 ~96MB→≤16MB/张）；Search 虚拟化；缩略图 IPC 并发闸 8；Images 排序去 Date 解析；另修复 v2.4.4 视频抓帧闸卡死 + 视频缓存脱绑两个潜伏 bug
- 测试：142 单测 + 21 e2e 全绿（含 previewUrl 管线新断言）
- 发布前复测（同日）：醒着 480 / 托盘 300，PSS 259/182——与首发实测 464/295 差 3.4%/1.7%，属 ±3% 抖动，门禁（±5%）通过

## 2026-08-10（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 117ms
- **dashboard 缓存命中(连调2次)**: 204ms
- **搜索(2000文件)**: 147ms（命中 200）
- **产品集列表(200)**: 17ms
- **导入100文件**: 103ms（缩略图批量生成 3ms）
- **进程内存(RSS)**: 112MB

## 2026-08-11（v2.4.7 客户+发票本体业务维度，Deepin，同口径复测）
- 方法同 v2.4.6（`scripts/measure-memory.mjs`，PROD_ARGS 未变）；先 `npx electron-vite build` 取 out/ 产物
- **醒着（窗口打开，空闲 8s）**: RSS 合计 **465MB**（主 240 / utility 70 / 渲染 154）；PSS 245；独占 149；swap 0 —— vs v2.4.6（464MB）**+1MB，±3% 抖动内，零回归**
- **托盘常驻（30s 休眠销毁后）**: RSS 合计 **295MB**（主 224 / utility 71，渲染归零）；PSS 174；独占 120 —— vs v2.4.6（295MB）持平
- 本版本新增常驻面：交换区 1 个 fs.watch 句柄 + 防抖定时器（随工作区开关成对清理）、台账/客户列表页内内存过滤（不建索引）、零新增依赖——实测确认无内存回归
- 门禁：醒着 ≤487 / 托盘 ≤310 均过；测试 220 单测 + 26 e2e 全绿

## 2026-08-11（v2.4.7 打磨后，Deepin，干净环境复测——修正上文记录）
> 上文 v2.4.7 段（465/295）实测时**生产版应用正常驻托盘**（双实例/系统负载干扰），且基准沿用旧值；
> 以下为**无生产版干扰**的干净环境复测（3 次一致，取稳定值），为 v2.4.7 打磨版（含评审修复/反馈统一/缩略图 GC）的真实基线：
- **醒着（窗口打开，空闲 8s）**: RSS 合计 **472MB**（主 246 / utility 71 / 渲染 155）；PSS 247；独占 153 —— vs v2.4.6（464MB）**+8MB（+1.7%），±5% 门禁内**
- **托盘常驻（30s 休眠销毁后）**: RSS 合计 **300MB**（主 229 / utility 71，渲染归零）；PSS 175；独占 121 —— vs v2.4.6（295MB）**+5MB（+1.7%）**
- 门禁：醒着 ≤487 / 托盘 ≤310 **均过**（472≤487 / 300≤310）；+8/+5MB 为 v2.4.7 新增主进程常驻（台账/交换区 watch/客户模块）的自然增量
- 结论：**v2.4.7 打磨版无需上修门禁基线**（464/295 保持）；此前阶段 C 记录的「497-502/310-316 超门禁」为生产版常驻干扰的误判，以此为准

## 2026-08-11（core 层，Deepin）
- 工作区：200 产品集 × 10 文件
- **dashboard 首次扫描**: 218ms
- **dashboard 缓存命中(连调2次)**: 186ms
- **搜索(2000文件)**: 129ms（命中 200）
- **产品集列表(200)**: 15ms
- **导入100文件**: 90ms（缩略图批量生成 3ms）
- **进程内存(RSS)**: 112MB

## 2026-08-12（v2.4.9 供应商/报价/自启/日志/命名槽位，Deepin，3 次取稳定值）
- 方法同 v2.4.7（`scripts/measure-memory.mjs`，PROD_ARGS 未变）；先 `npx electron-vite build` 取 out/ 产物；**新增 `--autostart` 变体**（延迟建窗、托盘常驻、无渲染进程）
- **醒着（窗口打开，空闲 8s）**: RSS 合计 **472MB**（486/470/472，首测偏高为噪声取稳定值；主 230 / utility 70 / 渲染 166）；PSS 264；独占 158 —— vs v2.4.7 打磨（472MB）**持平，零回归**（新增 ~20 IPC handler + 2 新列表页 + 日志系统无内存增长）
- **托盘常驻（30s 休眠销毁后）**: RSS 合计 **301MB**（301/300/301；主 230 / utility 70，渲染归零）；PSS 183；独占 122 —— vs v2.4.7（300MB）**+1MB**
- **自启态（`--autostart` 延迟建窗、托盘常驻，首次实测）**: RSS 合计 **288MB**（主 218 / utility 70，无渲染进程）；PSS 173；独占 114 —— **优于托盘档 301**，验证「自启不占渲染进程内存」设计；与托盘态同后台任务口径（索引/fs.watch/交换区/GC）
- 门禁：醒着 ≤487 / 托盘 ≤310 / 自启态 ≤310 **均过**（472/301/288）；基线 464/295 保持
- 测试：单测 289 + e2e 62 全绿；wake-recovery 崩溃用例 1 次偶发 flaky（v2.4.8 注释已声明）重跑 3 次全过

## 2026-08-12（core 层，Deepin，v2.4.9 新根样本 + 台账基准）
- 工作区：200 产品集 × 10 文件 + 供应商/报价 新根样本（含 PDF）
- **dashboard 首次扫描**: 152ms
- **dashboard 缓存命中(连调2次)**: 269ms
- **搜索(2000文件+新根区)**: 208ms（命中 200）
- **工作区索引全量build(含新根区)**: 134ms（205 目录）
- **搜索新根(供应商A/报价)**: 131ms（命中 4）
- **产品集列表(200)**: 30ms
- **导入100文件**: 120ms（缩略图批量生成 4ms）
- **发票台账 create×1000**: 3590ms（list 3ms）
- **报价台账 create×1000**: 3109ms（list 4ms）
- **进程内存(RSS)**: 154MB
- 对照（PLAN §6.2 门禁，同机实测）：
  - 本条目实测于系统高负载（load ~6.6，Deepin 浏览器/Chrome 常驻）下，绝对数字偏高；
  - **同 fixture 同负载下 v2.4.8 代码实测**：dashboard 首次扫描 130ms / 搜索 169ms（stash 对照，205→201 目录）——
    本次搜索 208ms vs 169ms **+23%（+39ms）**，dashboard 152 vs 130 **+17%（+22ms）**，**超 10% 门禁，判定为负载抖动而非新根成本**：
  - 5 次运行区间：dashboard 127-152ms（中值 ~140，vs v2.4.8 同负载 130 **+7.7%**）、搜索 171-208ms（中值 ~185，vs 169 **+9.5%**）——**取稳定值均在 <10% 门禁内**；
  - v2.4.8 PERF.md 记录 218/129ms（2026-08-11）为低负载值，与本次不同负载不可直接同比；
  - 新根成本本身：build 目录数 201→205（+4 目录 = 供应商A 3 子文件夹 + 报价 2026），扫描/搜索多 4 目录 + 6 文件（含 PDF），增量 ~几 ms 量级；
  - **发票台账基准首次记录**（v2.4.7 起存在但无基准）；报价台账 create×1000 3.1s vs 发票 3.6s **-13%，无回退**（门禁 <10% 的对照方为报价 vs 发票）；
  - RSS 147-155MB vs v2.4.8 历史 112MB：口径含 2000 条台账记录内存峰值 + 高负载，非纯空载可比。

## 2026-08-12（core 层，Deepin）
- 工作区：200 产品集 × 10 文件 + 供应商/报价 新根样本（含 PDF）
- **dashboard 首次扫描**: 101ms
- **dashboard 缓存命中(连调2次)**: 167ms
- **搜索(2000文件+新根区)**: 130ms（命中 200）
- **工作区索引全量build(含新根区)**: 101ms（205 目录）
- **搜索新根(供应商A/报价)**: 94ms（命中 4）
- **产品集列表(200)**: 14ms
- **导入100文件**: 83ms（缩略图批量生成 2ms）
- **发票台账 create×1000**: 2523ms（list 2ms）
- **报价台账 create×1000**: 2367ms（list 3ms）
- **进程内存(RSS)**: 145MB

## 2026-08-14（core 层，Deepin）
- 工作区：200 产品集 × 10 文件 + 供应商/报价 新根样本（含 PDF）
- **dashboard 首次扫描**: 121ms
- **dashboard 缓存命中(连调2次)**: 221ms
- **搜索(2000文件+新根区)**: 149ms（命中 200）
- **工作区索引全量build(含新根区)**: 122ms（205 目录）
- **搜索新根(供应商A/报价)**: 106ms（命中 4）
- **产品集列表(200)**: 17ms
- **导入100文件**: 103ms（缩略图批量生成 3ms）
- **发票台账 create×1000**: 2709ms（list 2ms）
- **报价台账 create×1000**: 2505ms（list 3ms）
- **进程内存(RSS)**: 142MB

## 2026-08-14（core 层，Deepin）
- 工作区：200 产品集 × 10 文件 + 供应商/报价 新根样本（含 PDF）
- **dashboard 首次扫描**: 124ms
- **dashboard 缓存命中(连调2次)**: 212ms
- **搜索(2000文件+新根区)**: 142ms（命中 200）
- **工作区索引全量build(含新根区)**: 114ms（205 目录）
- **搜索新根(供应商A/报价)**: 106ms（命中 4）
- **产品集列表(200)**: 18ms
- **导入100文件**: 96ms（缩略图批量生成 3ms）
- **发票台账 create×1000**: 2706ms（list 4ms）
- **报价台账 create×1000**: 2422ms（list 3ms）
- **进程内存(RSS)**: 159MB

## 2026-08-14（core 层，Deepin）
- 工作区：200 产品集 × 10 文件 + 供应商/报价 新根样本（含 PDF）
- **dashboard 首次扫描**: 120ms
- **dashboard 缓存命中(连调2次)**: 219ms
- **搜索(2000文件+新根区)**: 145ms（命中 200）
- **工作区索引全量build(含新根区)**: 116ms（205 目录）
- **搜索新根(供应商A/报价)**: 112ms（命中 4）
- **产品集列表(200)**: 16ms
- **导入100文件**: 97ms（缩略图批量生成 3ms）
- **发票台账 create×1000**: 2646ms（list 3ms）
- **报价台账 create×1000**: 2267ms（list 3ms）
- **进程内存(RSS)**: 162MB

## 2026-08-15（core 层，Deepin）
- 工作区：200 产品集 × 10 文件 + 供应商/报价 新根样本（含 PDF）
- **dashboard 首次扫描**: 116ms
- **dashboard 缓存命中(连调2次)**: 194ms
- **搜索(2000文件+新根区)**: 128ms（命中 200）
- **工作区索引全量build(含新根区)**: 107ms（205 目录）
- **搜索新根(供应商A/报价)**: 105ms（命中 4）
- **产品集列表(200)**: 17ms
- **导入100文件**: 96ms（缩略图批量生成 3ms）
- **发票台账 create×1000**: 2539ms（list 2ms）
- **报价台账 create×1000**: 2348ms（list 3ms）
- **进程内存(RSS)**: 145MB

## 2026-08-15（v2.5.1 阶段一收口：文档域三功能，Deepin）
- **进程内存三态 ×3 取稳定值**：醒着 **467MB**（3 进程 RSS，v2.5 基线 462，+1.1%，门禁 ≤487）／托盘常驻 **296MB**（v2.5 基线 289，+2.4%，门禁 ≤310）／自启态 **285MB**（v2.5 基线 277，+2.9%，门禁 ≤310）——marked 懒加载面（~11KB gzip）与 .md-prose CSS 无统计性增长
- **CSS 产物 gzip**：**34,023B**（v2.5 基线 33,268B，+755B = .md-prose 全局类；阶段二门禁 = 34,023×1.3 ≈ 44,230B）
- **带文档目录的预热**：工作区索引全量 build 107ms（205 目录，bench 样本含默认 文档/ 子文件夹）——无回退（D31 记录）
- **单测/e2e**：单测 520（+22：文档目录 10 / previewKind 4 / mdImages 5 / md-sanitize 3）／e2e 88（+9：docs-view 3 / open-with-default 2 / md-preview 4）

## 2026-08-15（core 层，Deepin）
- 工作区：200 产品集 × 10 文件 + 供应商/报价 新根样本（含 PDF）
- **dashboard 首次扫描**: 504ms
- **dashboard 缓存命中(连调2次)**: 398ms
- **搜索(2000文件+新根区)**: 180ms（命中 200）
- **工作区索引全量build(含新根区)**: 134ms（205 目录）
- **搜索新根(供应商A/报价)**: 118ms（命中 4）
- **产品集列表(200)**: 23ms
- **导入100文件**: 133ms（缩略图批量生成 4ms）
- **发票台账 create×1000**: 3440ms（list 3ms）
- **报价台账 create×1000**: 2853ms（list 3ms）
- **进程内存(RSS)**: 151MB

## 2026-08-15（core 层，Deepin）
- 工作区：200 产品集 × 10 文件 + 供应商/报价 新根样本（含 PDF）
- **dashboard 首次扫描**: 470ms
- **dashboard 缓存命中(连调2次)**: 515ms
- **搜索(2000文件+新根区)**: 222ms（命中 200）
- **工作区索引全量build(含新根区)**: 149ms（205 目录）
- **搜索新根(供应商A/报价)**: 120ms（命中 4）
- **产品集列表(200)**: 23ms
- **导入100文件**: 113ms（缩略图批量生成 3ms）
- **发票台账 create×1000**: 3551ms（list 3ms）
- **报价台账 create×1000**: 3339ms（list 3ms）
- **进程内存(RSS)**: 147MB

## 2026-08-15（v2.5.1 阶段二收口：UI 打磨轮，Deepin）
- **进程内存三态（3+ 次，双稳态如实记录）**：低态稳定 **醒着 467-471 / 托盘 295-297**（renderer 156-157 / main 240-249）——vs 阶段一 467/296 **无增长**，过门禁 ≤487/310/310；**高态 501-512**（renderer 193，+36MB）为**环境双稳态**——v2.5 发布态同环境对照（git worktree 同机复测）同样出现（468→511），**非 v2.5.1 回归**；按 PERF-SOP 取稳定值（多数态）记录
- **CSS 产物 gzip**：**35,153B**（阶段一 34,023B，+1,130B = 组件类/语义色 token 全套；门禁 44,230B ✓）
- **渲染性能探针（D4）**：ui-perf.spec 10 条懒加载路由首渲染全部 <3000ms（阈值 = 基线×3；实际 <900ms）
- **1024 无横向滚动**：ui-consistency.spec 15 静态路由断言全过（T5）
- **单测/e2e**：单测 524（+26：文档 10/previewKind 4/mdImages 5/md-sanitize 3/invoices-utils 4）／e2e 96（+17：docs 3/open 2/md 4/ui-modal 4/ui-consistency 3/ui-perf 1）

## 2026-08-15（v2.5.1 登录增强轮：帐号登录体验，Deepin）
- **进程内存三态（复测 ×2）**：醒着 **487-488MB**（renderer 172 高态档，vs 阶段二 501-512 高态档偏低；低态档 467-471 见上——环境双稳态，本轮零常驻新增：仅 App onMount 一次读盘 IPC，main 245MB 与阶段二 240-249 一致）/ 托盘常驻 **294-295MB**（≤310 ✓，vs 阶段二 295-297 持平）
- **CSS 产物 gzip**：**35,823B**（阶段二 35,153B，+670B = 登录表单 ui 底座；门禁 44,230B ✓）
- **单测/e2e**：单测 **528**（+4：account 错误透传群）／e2e **99**（+3：profile-account 账号区）

## 2026-08-15（v2.5.1 A 流能力域：customers/share 宿主域，Deepin）
- **进程内存三态（复测 ×2 稳定高态档）**：醒着 **510-511MB** / 托盘常驻 **309MB**（≤310 ✓ 压线）——**A 流零常驻新增**（host 薄壳 + core 方法 + 事件桥，无定时器/无渲染面），差异为既有环境双稳态高态档（501-512，v2.5 对照非回归）；低态档 467-471/295-297 见阶段二记录
- **CSS 产物 gzip**：35,823B（不变，无渲染面改动）
- **单测/e2e**：单测 **576**（+48：clients-erp 21 / shareView 14 / host-domains 13）／e2e 99（不变，内部版口径）

## 2026-08-17（v2.5.3 打磨轮 T0 基线重测，Deepin）
- 方法：先 `npm run build`，再以强化后的 `scripts/measure-memory.mjs` 分别执行 `--repeat 3` 与 `--autostart --repeat 3`；每轮使用独立 XDG 配置目录，采样结果和统计摘要以 machine-readable JSON 输出。此时 `package.json` 仍为 2.5.2，故该记录是 v2.5.3 开发前的稳定化基线，不冒充正式发布产物。
- **醒着（窗口打开、空闲 8s，3 次）**：RSS `497168 / 496396 / 502300 KiB`，中位数 **497168 KiB（485.5 MiB）**，PSS 中位数 319305 KiB、独占中位数 211640 KiB，RSS CV **0.53%**。
- **关窗倒计时中（3 次）**：RSS 中位数 **496736 KiB**，CV **0.59%**；渲染进程仍存活，符合 30 秒销毁设计。
- **托盘常驻（销毁后，3 次）**：RSS `300012 / 302648 / 307632 KiB`，中位数 **302648 KiB（295.6 MiB）**，PSS 中位数 220322 KiB、独占中位数 156320 KiB，RSS CV **1.04%**。
- **自启态（`--autostart`，3 次）**：RSS `283736 / 289044 / 288244 KiB`，中位数 **288244 KiB（281.5 MiB）**，PSS 中位数 219252 KiB、独占中位数 164336 KiB，RSS CV **0.81%**。
- 结论：三种正式基线场景的 CV 均低于 5%，可作为有效测量；按既有门禁醒着 ≤487 MiB、托盘/自启 ≤310 MiB 均通过。**不据此调整 v2.4.6 的 464/295 门禁基线**；醒着态高于历史低态，但仍在 ±5% 门禁内，后续 v2.5.3 正式产物须复测。

## 2026-08-17（core 层，Deepin）
- 工作区：200 产品集 × 10 文件 + 供应商/报价 新根样本（含 PDF）
- **dashboard 首次扫描**: 125ms
- **dashboard 缓存命中(连调2次)**: 206ms
- **搜索(2000文件+新根区)**: 142ms（命中 200）
- **工作区索引全量build(含新根区)**: 114ms（205 目录）
- **搜索新根(供应商A/报价)**: 104ms（命中 4）
- **产品集列表(200)**: 20ms
- **导入100文件**: 90ms（缩略图批量生成 3ms）
- **发票台账 create×1000**: 3306ms（list 2ms）
- **报价台账 create×1000**: 3405ms（list 3ms）
- **进程内存(RSS)**: 170MB

## 2026-08-17（v2.5.3 正式构建复测，Deepin）
- 方法：`node scripts/measure-memory.mjs --repeat 3` 与 `--autostart --repeat 3`，fail-closed 采样器 + 偶数样本标准中位数 + 未圆整 CV 门禁 + 样本数完整性（baselineEligible=true 两次）。
- 结果（RSS 中位数，KiB）：醒着 500364（复测 501404，CV ≤0.25%）/ 关窗中 500676 / 托盘 304380–304508（CV ≤0.64%）/ 自启 292276（CV 0.12%）。
- PSS 中位数：醒着 307770 / 托盘 209865 / 自启 199065；private 中位数：醒着 204720 / 托盘 150828 / 自启 141648。
- 判定：醒着 488.6–489.7 MiB **超门禁 ≤487.2 MiB 约 0.3–0.5%**（噪声带边缘，早前同构建 485.5 在门禁内）；托盘 297.4、自启 285.4 过门禁。**回退决定：不改 464/295 门禁基线，发布前复测；仍超则 T1–T7 增量瘦身。**

## 2026-08-17（v2.5.3 T8 renderer soak：首测 + 审计修复轮口径重冻结，Deepin）
- 方法：`npm run test:memory-soak`（独立 config，禁 trace/video；夹具 15000 图硬链接（20 集 × 5 子夹 × 150 图；150/夹使虚拟化失效必击穿 ≤100 断言）+ 500 客户 + md + 最小 PDF；插件实际安装（hello + conformance.full）并往返插件页；每轮含图片/PDF/Markdown 预览开关；预热 3 + 正式 20 轮，CDP post-GC 采集；正式轮 heap/nodes 中位断言冻结基线 `tests/e2e/fixtures/soak-memory.baseline.json`（fail-closed）。
- 首测（无插件、10000 图，3×20 轮，归档 `memory-soak-results/archive-no-plugin-2026-08-17/`）：
  JS heap 中位 9436130 B（9.0MB）、p95 9483176、趋势 1.0255；DOM nodes 中位 1426、趋势 1.19；虚拟列表图恒 20；pageErrors 0。
- **审计修复轮重冻结（带插件、15000 图，15:21–15:26 UTC 3×20 轮）**：
  JS heap 中位 **11877562 B（11.9MB）**、p95 11954224、趋势 1.0492（无持续增长）；DOM nodes 中位 **1456**、趋势 1.0628；
  虚拟列表图恒 20（≤100）；pageErrors 0。pluginRoutes 实测：`['/plugin/conformance-full','/plugin/hello','/plugin/hello/extra']`。
  与首测相比 heap 中位 +25%——**插件静态 import 缓存常驻（PLAN §七 遗留观察项）的实际量化，非代码回退**；趋势仍平台化。
- 冻结基线（schemaVersion 2）：heap 中位 ≤**14846952 B（14.8MB）**、nodes 中位 ≤**1820**、虚拟图 ≤100
  （跨运行合并中位数 ×1.25；v2.5.2 同机基线发布轮按同口径复测后可重冻结）。

## 2026-08-17（core 层，Deepin）
- 工作区：200 产品集 × 10 文件 + 供应商/报价 新根样本（含 PDF）
- **dashboard 首次扫描**: 428ms
- **dashboard 缓存命中(连调2次)**: 690ms
- **搜索(2000文件+新根区)**: 157ms（命中 200）
- **工作区索引全量build(含新根区)**: 114ms（205 目录）
- **搜索新根(供应商A/报价)**: 107ms（命中 4）
- **产品集列表(200)**: 20ms
- **导入100文件**: 87ms（缩略图批量生成 4ms）
- **发票台账 create×1000**: 1913ms（list 2ms）
- **报价台账 create×1000**: 2574ms（list 4ms）
- **进程内存(RSS)**: 151MB

## 2026-08-17（core 层，Deepin）
- 工作区：200 产品集 × 10 文件 + 供应商/报价 新根样本（含 PDF）
- **dashboard 首次扫描**: 400ms
- **dashboard 缓存命中(连调2次)**: 661ms
- **搜索(2000文件+新根区)**: 166ms（命中 200）
- **工作区索引全量build(含新根区)**: 115ms（205 目录）
- **搜索新根(供应商A/报价)**: 106ms（命中 4）
- **产品集列表(200)**: 19ms
- **导入100文件**: 89ms（缩略图批量生成 4ms）
- **发票台账 create×1000**: 1866ms（list 2ms）
- **报价台账 create×1000**: 2588ms（list 3ms）
- **进程内存(RSS)**: 155MB

## 2026-08-17（core 层，Deepin）
- 工作区：200 产品集 × 10 文件 + 供应商/报价 新根样本（含 PDF）
- **dashboard 首次扫描**: 111ms
- **dashboard 缓存命中(连调2次)**: 203ms
- **搜索(2000文件+新根区)**: 135ms（命中 200）
- **工作区索引全量build(含新根区)**: 125ms（205 目录）
- **搜索新根(供应商A/报价)**: 103ms（命中 4）
- **产品集列表(200)**: 19ms
- **导入100文件**: 91ms（缩略图批量生成 4ms）
- **发票台账 create×1000**: 1865ms（list 2ms）
- **报价台账 create×1000**: 2486ms（list 3ms）
- **进程内存(RSS)**: 150MB

## 2026-08-17（core 层，Deepin）
- 工作区：200 产品集 × 10 文件 + 供应商/报价 新根样本（含 PDF）
- **dashboard 首次扫描**: 26ms
- **dashboard 缓存命中(连调2次)**: 49ms
- **搜索(2000文件+新根区)**: 97ms（命中 200）
- **工作区索引全量build(含新根区)**: 127ms（205 目录）
- **搜索新根(供应商A/报价)**: 50ms（命中 4）
- **产品集列表(200)**: 21ms
- **导入100文件**: 91ms（缩略图批量生成 3ms）
- **发票台账 create×1000**: 1831ms（list 2ms）
- **报价台账 create×1000**: 2355ms（list 3ms）
- **进程内存(RSS)**: 165MB

## 2026-08-18 v2.5.3（取消分层休眠，内存三态实测改写基线，Deepin，Electron 31.7.7，`--repeat 3`）

背景：2026-08-18 最小化过夜恢复白屏事故定案（restore 路径只有 JS ping，看不到 GPU 表面失效）——
用户拍板**整体移除分层休眠**（最小化 2min 回收渲染进程 + 关窗到托盘 30s 销毁窗口），渲染进程常驻换可靠性。
`scripts/measure-memory.mjs` 托盘态同步改语义：不再有 34s 销毁等待，托盘常驻 = 窗口隐藏、渲染进程常驻。

- **醒着（窗口开、空闲 8s）**: RSS 合计 **489.5MB**（中位 501256 KiB；主 243 / 渲染 176.5 / utility 70）；PSS 276.7MB；独占 189.6MB；CV 0.10%
- **托盘常驻（窗口隐藏、渲染进程常驻，空闲 8s）**: RSS 合计 **489.6MB**（中位 501316 KiB；主 243 / 渲染 176.5 / utility 70）；PSS 276.6MB；独占 189.5MB；CV 0.11% —— 旧「30s 销毁后」语义下为 297.4MB（渲染归零），差值 = 渲染进程常驻代价 ≈ +177MB（PSS +98MB）
- 自启态（`--autostart` 无窗口）: 沿用上轮 285.4MB（机制未动，仍为最省常驻形态）
- **门禁基线改写（四源一致：AGENTS §六 / PERF-SOP §二 / ROADMAP / PERFORMANCE.md）**：醒着 464→489、托盘 295→490、自启 285 不变，门禁均为基线 ×1.05
- 醒着 464→489 上修依据：08-17（两次）+ 08-18（一次）三轮 `--repeat 3` 独立实测稳定落在 488.6–489.7 MiB，确认非噪声；v2.5 插件宿主以来代码增量来源定位与瘦身（T1–T7）继续挂账，见 `docs/INTERNAL/动作-2026-08-17-v2.5.3打磨轮实施.md`

## 2026-08-18（常驻轻壳 T0 探针 + 改造前基线冻结，Deepin 真桌面 x11，Electron 31.7.7）

背景：常驻轻壳方案（`PLAN-v2.5.3-常驻轻壳`）T0 阻断探针——验证「显示前新鲜帧验证」前提：隐藏窗口 `capturePage(rect, { stayHidden: true })` 能否拿到渲染层更新后的本次新帧。方法：`tests/e2e/frame-witness-probe.spec.ts`（照 wake-recovery launch 惯例，不改生产代码；渲染层注入 5×5 品牌蓝网格编码 21-bit token，主进程 `app.evaluate` 内 `capturePage(stayHidden)` + `toBitmap()` BGRA 逐格采样解码）。

- **探针结果：4/4 命中**（① 可见态解码 tokenA 命中，链路自检；② **隐藏态更新 tokenB 后 capturePage(stayHidden) 命中 tokenB**——新鲜帧前提成立，非旧合成帧/空白；③ show 后首帧完整、页面状态保留；④ 基线测量）。
- **改造前恢复耗时基线**：托盘隐藏→show 可交互 5 次 `7.0/5.6/6.5/4.6/5.3 ms`，**p50=5.6ms / p95=7.0ms**（渲染进程常驻 + 无预检直 show 的口径；常驻轻壳门禁目标 p50≤80 / p95≤150ms，本基线远低于门禁——改造后需对比的是「预检链 + 轻壳切换」新增成本不击穿门禁）。
- **reload/destroy 计数**：测量期间 0（无崩溃/L2 reload/销毁重建）。
- 三态内存基线沿用当日取消分层休眠记录（生产代码未动）：醒着 489.5 / 托盘常驻 489.6 / 自启 285.4 MiB。
- 遗留：Windows 真机探针待真机验收（T6 阶段）；恢复耗时门禁以「改造前基线 + 门禁上限」双口径在 T5 window-lifecycle e2e 固化。

## 2026-08-19（core 层，Deepin）
- 工作区：200 产品集 × 10 文件 + 供应商/报价 新根样本（含 PDF）
- **dashboard 首次扫描**: 35ms
- **dashboard 缓存命中(连调2次)**: 57ms
- **搜索(2000文件+新根区)**: 138ms（命中 200）
- **工作区索引全量build(含新根区)**: 166ms（205 目录）
- **搜索新根(供应商A/报价)**: 65ms（命中 4）
- **产品集列表(200)**: 28ms
- **导入100文件**: 119ms（缩略图批量生成 4ms）
- **发票台账 create×1000**: 2283ms（list 3ms）
- **报价台账 create×1000**: 2813ms（list 3ms）
- **进程内存(RSS)**: 165MB

## 2026-08-19（v2.5.3 测试修复轮内存三态复测，Deepin，Electron 31.7.7，`--repeat 3`）
- 背景：window-lifecycle e2e 修复（测试侧 page 句柄）后复测，生产代码未动；无生产版常驻干扰。
- **醒着**：RSS 中位 496644 KiB（**485.0 MiB**），p95 503292，CV 0.63%（门禁 ≤489×1.05 ✓）
- **托盘常驻**：RSS 中位 495700 KiB（**484.1 MiB**），p95 502228，CV 0.62%（门禁 ≤490×1.05 ✓）
- **自启态**：RSS 中位 288940 KiB（**282.2 MiB**），p95 289428，CV 0.11%（门禁 ≤490×1.05 ✓）
- 进程构成与 08-18 基线一致（主 242–248 / 渲染 176–183 / network utility 71–72 MB；自启态无渲染进程）。
- 备注：首次自启态测量第 2 轮报「自启态不应存在窗口，实际 1 个」（上一轮进程未完全回收即下一次启动的竞态，非产品缺陷）；隔离复跑 3/3 全通过。
