# 启禾文件管理 - 使用帮助

## 一、安装与启动

前往官网 `https://www.qihebook.cloud/file-manager` 下载对应平台的安装包（无需验证码）：

### Windows

1. 下载 Windows 安装包（NSIS 安装程序，文件名形如 `启禾文件管理 Setup 2.x.x.exe`）。
2. 双击运行安装程序，按向导完成安装。
3. 从开始菜单或桌面快捷方式启动「启禾文件管理」。
4. 如需卸载，在「设置 → 应用 → 已安装的应用」中找到「启禾文件管理」卸载即可。

### Linux

1. 下载 AppImage 或 deb 安装包。
2. AppImage：`chmod +x` 后直接运行（免安装）；deb：`sudo dpkg -i qihe-box_x.x.x_amd64.deb` 安装（文件名中的版本号以实际下载为准）。
3. 从应用菜单或终端启动「启禾文件管理」。

## 二、首次使用

### 自动创建默认工作区

首次启动应用时**无需任何手动操作**：应用会自动在用户主目录下创建默认工作区（Windows 为 `C:\Users\你的用户名\启禾文件管理`，Linux 为 `~/启禾文件管理`）并直接打开；之后每次启动会自动恢复最近使用的工作区。

### 切换工作区

1. 点击顶部「选择工作区」。
2. 「新建工作区」：在弹出的文件夹选择器中，选择一个空文件夹，应用会在该文件夹内创建必要的目录结构。
3. 「打开工作区」：选择已有的工作区文件夹。
4. 最近打开过的工作区会显示在菜单下方，点击即可切换。

> 提示：整个工作区是自包含的，复制到任何位置、任何电脑上都可以继续使用。

## 三、「我的」页面

「我的」页面集成了更新、帮助和隐私协议：

- **检查更新**：一键检查新版本；发现新版引导前往官网下载安装包（应用内暂不提供自动安装）。
- **使用帮助**：查看本文档。
- **隐私协议**：查看数据收集与隐私说明。

## 四、检查更新

应用会自动检查新版本：**启动时**与**每 24 小时**静默检查一次；也可打开「我的 → 检查更新」手动点击「检查更新」。

- 如果有新版本，界面会显示「发现新版本」，点击「前往官网下载」获取最新安装包。
- 下载后覆盖安装即可（Windows 安装包 / Linux AppImage + deb，见「一、安装与启动」）。

### 更新失败怎么办

- 应用内只做新版本检查与引导，不自动下载安装；如官网下载或安装异常，请检查网络后重试。
- 安装新版前请先退出正在运行的「启禾文件管理」，并保存好工作区数据。
- 覆盖安装不会影响工作区文件与元数据，可放心升级。

## 五、产品集

产品集是资料的分类单元，可以理解为一个客户、一个产品系列或一个项目。

### 新建产品集

1. 在左侧导航点击「产品集」。
2. 点击右上角「新建产品集」。
3. 输入产品集名称，可填写标签和备注。
4. 点击「确认创建」。

### 给产品集打标签

- 新建产品集时，在「标签」框中输入，多个标签用逗号分隔，例如：`客户, 重点`。
- 已创建的产品集，可右键点击卡片，选择「编辑信息」修改标签和备注。

### 筛选产品集

- 在产品集页面顶部，可按名称搜索。
- 也可按标签下拉筛选。

### 重命名产品集

- 进入某个产品集详情页，点击顶部面包屑中的产品集名称，即可编辑。
- 注意：只有**空产品集**（没有任何文件）才能重命名，避免数据关联断裂。

### 删除产品集

- 在产品集卡片上点击 🗑️ 图标，或右键选择「删除」。
- 删除会将产品集移入回收站，可在回收站恢复（彻底删除才不可恢复）。

## 六、添加图包（图片）

### 方法 1：拖拽导入

1. 进入某个产品集，点击「图包」入口。
2. 选择左侧子文件夹，如「主图」「详情页」「白底图」「素材」。
3. 将电脑中的图片文件直接拖入文件浏览器区域。
4. 文件会按命名模板自动重命名并保存到对应位置。

### 方法 2：通过产品集详情页导入

1. 在产品集详情页点击「图包」大卡片。
2. 进入后使用拖拽或复制粘贴方式添加图片。

### 查看图包

- 点击顶部「图包库」，可查看所有产品集下的所有图片。
- 支持按产品集、子文件夹筛选，按名称搜索，按修改时间/文件名/大小排序。
- 支持按**标签**筛选（v2.4.4）：点顶部标签下拉选择，只看带指定标签的文件。
- **图片 / 视频聚合**（v2.4.4）：同一个图包库里可以同时存放图片与视频（如实拍视频、安装演示），筛选栏可切换「全部 / 仅图片 / 仅视频」，视频卡片自动抓帧生成缩略图，点击即可预览播放。

### 缩略图

- 导入图片时会自动生成 256×256 的缩略图；PDF 证书以点开预览查看内容（pdfjs 渲染，支持翻页/缩放）。
- 缩略图缓存在系统用户数据目录（`~/.config/启禾文件管理/thumbs`，不进工作区/坚果云同步），旧版工作区缓存自动迁移复用。

## 七、添加证书

### 导入证书

1. 进入某个产品集，点击「证书」入口。
2. 选择左侧子文件夹，如「3C」「质检」「专利」。
3. 将 PDF 或图片格式的证书文件拖入文件浏览器区域。

### 查看证书

- 点击顶部「证书库」，可查看所有产品集下的所有证书。
- 支持筛选、搜索、排序，与图包库相同。

### 证书元数据

1. 在文件浏览器中点击文件卡片预览。
2. 右键点击文件，选择「编辑信息」。
3. 可填写：证书类型、到期日、标签、备注。
4. 填写到期日后，仪表盘会提醒即将过期的证书。

## 八、文件预览

1. 在文件浏览器、图包库或证书库中，**单击选中、双击打开**文件卡片即可预览。
2. 支持图片、PDF、视频预览。PDF 预览基于 pdfjs-dist 官方查看器（v2.2.1）：
   - **连续滚动**浏览，鼠标滚轮直接翻页
   - **文字可选中复制**（文本层）
   - **全文搜索**（🔍 输入关键词，高亮显示 + 上下匹配跳转）
   - **缩放**：适配页面一键还原，或自定义 40%-400%
   - 底部显示当前页/总页数，可跳页
3. 预览弹窗中可：
   - 用系统程序打开
   - 删除文件
   - 编辑元数据（右键或详情页）

## 九、多选操作（v2.4.4）

在证书库、图包库、文件浏览器中，点击文件卡片左上角的 **☑ 勾选框** 即可选中（再点取消）；直接点卡片任意位置也可选中/取消。选中后卡片高亮，顶部出现操作栏：

- **全选 / 取消全选**：一键选择当前列表全部文件
- **复制**：把选中的原文件复制到系统剪贴板，到微信/钉钉按 `Ctrl+V` 粘贴即发
- **🏷️ 打标**：批量给选中文件打标签（弹窗输入，回车确认；未定义的新标签名自动创建）
- **📦 压缩分享**：打包选中文件（见「十、压缩分享与解压」）
- **在文件夹中显示 / 删除**：批量定位或移入回收站

右键任意选中的卡片会弹出同样的菜单（多选时作用于全部选中文件）：预览 / 编辑信息 / 复制 / 打标 / 压缩分享 / 批量重命名 / 删除。

## 十、压缩分享与解压（v2.4.4）

### 压缩分享

1. 多选一个或多个文件（见「九、多选操作」），点击操作栏「📦 压缩分享」（或右键 → 压缩分享）。
2. 应用将选中文件**流式打包**到工作区 `导出/` 目录（零新增依赖、恒定内存，打包过程不占额外磁盘）：
   - 选中文件全部属于同一个产品集 → 自动命名 `产品集名_分享.zip`
   - 来自多个位置 → 命名 `分享_时间戳.zip`；重名自动加序号
3. 打包进度实时显示，可随时取消（取消自动清理半成品）。
4. 完成后点「打开所在文件夹」或「复制到剪贴板」，即可把压缩包外发给客户 / 平台。

> 单个压缩包上限 **4GB / 65535 个文件**，超出会明确报错，请分批打包。

### 解压 zip

在文件浏览器中右键 `.zip` 文件，选择：

- **解压到当前文件夹**：直接解出全部文件
- **解压到 `<包名>/`**：解压到同名子文件夹，避免与现有文件混在一起

解压进度实时显示、可取消；遇到损坏文件会自动清理半成品，不影响其他条目。

## 十一、外发文件

### 复制到剪贴板

1. 在**文件浏览器**中选中一个或多个文件。
2. 点击「复制选中」按钮，或按 `Ctrl+C`（快捷键仅在文件浏览器中生效；图包库 / 证书库请使用「复制选中」按钮）。
3. 在微信、钉钉、邮件等窗口中按 `Ctrl+V` 粘贴发送原图/原文件。

### 在资源管理器中打开

1. 选中文件。
2. 点击「在文件夹中显示」。
3. 系统会打开资源管理器并高亮选中文件，可直接拖拽到聊天窗口。

### 右键菜单

在文件卡片上右键，可快速：

- 预览
- 编辑信息
- 复制
- 在文件夹中显示
- 删除

## 十二、搜索

### 全局搜索

1. 点击顶部搜索框（或按快捷键 `Ctrl+K` 快速聚焦），输入文件名或产品集名称。
2. 按回车进入搜索结果页。
3. 命中结果按「产品集 / 文件」两组展示，卡片上直接显示文件的**标签**（v2.4.4）；结果很多时列表自动按需渲染，滚动流畅不卡顿。

### 图包库 / 证书库搜索

- 在「图包库」或「证书库」页面顶部，可直接按文件名搜索。
- 证书库/图包库/文件浏览器都支持**标签筛选**（v2.4.4），与按名称搜索可叠加使用。

## 十三、账号登录（可选）

登录启禾账号后，应用会自动上报匿名活跃信息（设备标识、软件版本、最近使用时间），仅用于统计产品使用情况。**不登录完全不影响本地功能。**

1. 进入「我的」→「账号」。
2. 输入邮箱和密码登录；没有账号可点击「去官网注册」。
3. 登录后无需任何操作，活跃统计自动生效；可随时在「账号」页登出。

> **服务地址**：登录服务地址随安装包内置（v2.5.2 起），安装后即可使用。若提示「未配置服务器地址，登录不可用」，说明当前安装包未携带服务配置（如 CI 构建的测试包或旧版本）——请重新安装官网发布的最新安装包；自定义服务器地址可写入应用数据目录的 `server.json`（`{"apiBase": "https://…"}`）后重启应用。

## 十四、系统托盘

- 关闭窗口时，应用默认最小化到系统托盘，不退出。
- 右键托盘图标可：显示主界面、隐藏到托盘、退出。
- 要完全退出，请从托盘菜单选择「退出」。

### 托盘常驻（v2.5.3 起取消后台休眠）

- 关闭窗口后应用留在系统托盘后台运行，点击托盘图标或再次启动应用即**原样恢复**——页面与状态完整保留，不再有「重建窗口」过程。v2.4.9 起：从托盘打开固定回到仪表盘首页（冷启动仍恢复上次浏览的页面）。
- **窗口失焦 30 秒**：自动清理图像解码缓存，回到窗口后立即重新加载（唯一保留的后台省内存机制）。
- 后台常驻内存约 490MB（界面进程保持待命，换取即开即用与唤醒可靠）；如需彻底释放内存，请从托盘菜单选「退出」。开机自启且不打开窗口时，后台占用更低（约 285MB）。

> 文件、缩略图、元数据始终在磁盘上，与内存机制无关。

## 十五、数据与备份

- 所有数据都保存在工作区文件夹中。
- 重要资料建议定期备份整个工作区文件夹。
- 工作区可放在坚果云、OneDrive、NAS 等共享目录中，实现多机访问或团队轮流使用。

## 十六、回收站（v2.3.1）

- 删除的文件、子文件夹、产品集会先移入**回收站**，不会立即彻底删除。
- 侧边栏「回收站」可查看所有已删除内容：**恢复**（回到原位置，同名冲突自动加「-恢复N」）或**彻底删除**（不可恢复）。
- 恢复后文件的标签、备注、缩略图原样保留；产品集恢复后 tags/备注也保留。
- 回收站数据保存在工作区 `.qihefilemanager/trash/`，备份工作区时一并带上。
- 回收站条目自删除起**保留 30 天**，超期条目在应用启动时自动彻底清理（连同对应元数据与缩略图缓存一并删除，不留残留）；如需提前清理，可在回收站中手动「彻底删除」。

## 十七、常见问题

**Q：为什么有些图片没有缩略图？**
A：缩略图是**按需生成**的：浏览到图片时自动生成，缩略图功能加入前导入的历史图片在首次浏览时也会自动补生成，无需重新导入。若某张图始终显示占位图，通常是文件损坏或格式不支持，可尝试重新导入该文件。

**Q：产品集为什么不能重命名？**
A：只有空产品集才能重命名。如果已导入文件，请先删除文件，或新建一个空产品集。

**Q：为什么复制到剪贴板后微信显示「图片未下载」？**
A：请使用「复制选中」功能（`CF_HDROP` 格式），然后到聊天窗口按 `Ctrl+V` 粘贴。不要直接拖拽图片卡片。

**Q：如何在另一台电脑上使用？**
A：把整个工作区文件夹复制到另一台电脑，然后用「打开工作区」选择它即可。

**Q：为什么提示有新版本，但没有自动更新？**
A：应用内只做新版本检查与引导，不自动下载安装。请点击「前往官网下载」获取最新安装包覆盖安装。

## 十八、收费与开源承诺

**本体永远免费开源**：本地文件管理核心（索引、预览、产品集、命名模板等）永久免费，源码开源（Apache License 2.0），无需激活、无需联网、无功能锁。

**插件与增值服务单独计价（提前说明）**：AI、云端加密备份、加密分享等进阶能力，未来将以**插件/订阅**形态按需交付（插件 = 能力包，订阅 = 许可证）。收费只针对插件与增值服务，**不改变本体免费承诺**。任何插件商业模式上线前，都会在此文档与官网提前公示。
