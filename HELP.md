# 启禾文件管理 - 使用帮助

## 一、安装与启动

1. 前往官网 `https://www.qihebook.cloud/box/` 下载 `qihefilemanager-amd64-installer.exe`；下载前需要输入网页弹出的图形验证码。
2. 双击运行安装程序，按向导完成安装。
3. 从开始菜单或桌面快捷方式启动「启禾文件管理」。
4. 如需卸载，在「设置 → 应用 → 已安装的应用」中找到「启禾文件管理」卸载即可。

> 提示：安装过程需要管理员权限，静默更新时系统会弹出 UAC 授权提示。

## 二、首次使用

### 创建工作区

工作区是所有文件、配置和元数据的根目录。

1. 启动应用后，点击顶部「选择工作区」→「新建工作区」。
2. 在弹出的文件夹选择器中，选择一个空文件夹作为工作区。
3. 应用会在该文件夹内创建必要的目录结构。

### 打开已有工作区

1. 点击顶部「选择工作区」→「打开工作区」。
2. 选择已有的工作区文件夹。
3. 最近打开过的工作区会显示在菜单下方，点击即可切换。

> 提示：整个工作区是自包含的，复制到任何位置、任何电脑上都可以继续使用。

## 三、「我的」页面

「我的」页面集成了更新、帮助和隐私协议：

- **检查更新**：一键检查新版本、下载安装包、自动更新并重启。
- **使用帮助**：查看本文档。
- **隐私协议**：查看数据收集与隐私说明。

## 四、检查更新

1. 打开「我的」→「检查更新」。
2. 点击「检查更新」按钮。
3. 如果有新版本，点击「下载更新」。
4. 下载完成后，点击「立即更新并重启」，应用会自动关闭、安装新版并重新启动。

### 更新失败怎么办

- 请确认当前 Windows 账号有管理员权限；静默安装需要写入安装目录。
- 安装前请关闭所有「启禾文件管理」窗口，并保存好工作区数据。
- 如果 UAC 提示后点击了「否」，安装会被取消，可重新检查更新再试一次。
- 若自动更新仍无法完成，可前往官网手动下载安装包覆盖安装。

## 五、产品集

产品集是资料的分类单元，可以理解为一个客户、一个产品系列或一个项目。

### 新建产品集

1. 在左侧导航点击「产品集」。
2. 点击右上角「新建产品集」。
3. 输入产品集名称，可填写标签和备注。
4. 点击「确认创建」。

### 给产品集打标签

- 新建产品集时，在「标签」框中输入，多个标签用逗号分隔，例如：`客户, 重点`。
- 已创建的产品集，可右键点击卡片，选择「编辑信息」修改标签和备注。

### 筛选产品集

- 在产品集页面顶部，可按名称搜索。
- 也可按标签下拉筛选。

### 重命名产品集

- 进入某个产品集详情页，点击顶部面包屑中的产品集名称，即可编辑。
- 注意：只有**空产品集**（没有任何文件）才能重命名，避免数据关联断裂。

### 删除产品集

- 在产品集卡片上点击 🗑️ 图标，或右键选择「删除」。
- 删除会同时删除该产品集下的所有文件和元数据，不可恢复。

## 六、添加图包（图片）

### 方法 1：拖拽导入

1. 进入某个产品集，点击「图包」入口。
2. 选择左侧子文件夹，如「主图」「详情页」「白底图」「素材」。
3. 将电脑中的图片文件直接拖入文件浏览器区域。
4. 文件会按命名模板自动重命名并保存到对应位置。

### 方法 2：通过产品集详情页导入

1. 在产品集详情页点击「图包」大卡片。
2. 进入后使用拖拽或复制粘贴方式添加图片。

### 查看图包

- 点击顶部「图包库」，可查看所有产品集下的所有图片。
- 支持按产品集、子文件夹筛选，按名称搜索，按修改时间/文件名/大小排序。

### 缩略图

- 导入图片时会自动生成 256×256 的缩略图；PDF 证书以点开预览查看内容（pdfjs 渲染，支持翻页/缩放）。
- 缩略图缓存在系统用户数据目录（`~/.config/启禾文件管理/thumbs`，不进工作区/坚果云同步），旧版工作区缓存自动迁移复用。

## 七、添加证书

### 导入证书

1. 进入某个产品集，点击「证书」入口。
2. 选择左侧子文件夹，如「3C」「质检」「专利」。
3. 将 PDF 或图片格式的证书文件拖入文件浏览器区域。

### 查看证书

- 点击顶部「证书库」，可查看所有产品集下的所有证书。
- 支持筛选、搜索、排序，与图包库相同。

### 证书元数据

1. 在文件浏览器中点击文件卡片预览。
2. 右键点击文件，选择「编辑信息」。
3. 可填写：证书类型、到期日、标签、备注。
4. 填写到期日后，仪表盘会提醒即将过期的证书。

## 八、文件预览

1. 在文件浏览器、图包库或证书库中，**左键点击**文件卡片即可预览。
2. 支持图片、PDF、视频预览。PDF 预览基于 pdfjs-dist 官方查看器（v2.2.1）：
   - **连续滚动**浏览，鼠标滚轮直接翻页
   - **文字可选中复制**（文本层）
   - **全文搜索**（🔍 输入关键词，高亮显示 + 上下匹配跳转）
   - **缩放**：适配页面一键还原，或自定义 40%-400%
   - 底部显示当前页/总页数，可跳页
3. 预览弹窗中可：
   - 用系统程序打开
   - 删除文件
   - 编辑元数据（右键或详情页）

## 九、外发文件

### 复制到剪贴板

1. 选中一个或多个文件。
2. 点击「复制选中」按钮，或按 `Ctrl+C`。
3. 在微信、钉钉、邮件等窗口中按 `Ctrl+V` 粘贴发送原图/原文件。

### 在资源管理器中打开

1. 选中文件。
2. 点击「在文件夹中显示」。
3. 系统会打开资源管理器并高亮选中文件，可直接拖拽到聊天窗口。

### 右键菜单

在文件卡片上右键，可快速：

- 预览
- 编辑信息
- 复制
- 在文件夹中显示
- 删除

## 十、搜索

### 全局搜索

1. 点击顶部搜索框，输入文件名或产品集名称。
2. 按回车进入搜索结果页。

### 图包库 / 证书库搜索

- 在「图包库」或「证书库」页面顶部，可直接按文件名搜索。

## 十一、账号登录（可选）

登录启禾账号后，应用会自动上报匿名活跃信息（设备标识、软件版本、最近使用时间），仅用于统计产品使用情况。**不登录完全不影响本地功能。**

1. 进入「我的」→「账号」。
2. 输入邮箱和密码登录；没有账号可点击「去官网注册」。
3. 登录后无需任何操作，活跃统计自动生效；可随时在「账号」页登出。

## 十二、系统托盘

- 关闭窗口时，应用默认最小化到系统托盘，不退出。
- 右键托盘图标可：显示主界面、隐藏到托盘、退出。
- 要完全退出，请从托盘菜单选择「退出」。

## 十三、数据与备份

- 所有数据都保存在工作区文件夹中。
- 重要资料建议定期备份整个工作区文件夹。
- 工作区可放在坚果云、OneDrive、NAS 等共享目录中，实现多机访问或团队轮流使用。


## 十四、常见问题

**Q：为什么有些图片没有缩略图？**
A：在缩略图功能加入前导入的图片不会自动补生成，重新导入一次即可。

**Q：产品集为什么不能重命名？**
A：只有空产品集才能重命名。如果已导入文件，请先删除文件，或新建一个空产品集。

**Q：为什么复制到剪贴板后微信显示「图片未下载」？**
A：请使用「复制选中」功能（`CF_HDROP` 格式），然后到聊天窗口按 `Ctrl+V` 粘贴。不要直接拖拽图片卡片。

**Q：如何在另一台电脑上使用？**
A：把整个工作区文件夹复制到另一台电脑，然后用「打开工作区」选择它即可。

**Q：更新下载完成后为什么没有安装成功？**
A：请确认当前账号有管理员权限，并在 UAC 提示时点击「是」。若权限不足或被安全软件拦截，可前往官网手动下载安装包覆盖安装。
