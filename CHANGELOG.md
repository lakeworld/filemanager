# 更新日志

## v2.4.6 — 2026-08-11（内存/性能优化轮，无新功能）

### 内存优化（托盘常驻 310 → 295MB，达成 ≤300MB 目标）

- **exceljs 改动态懒加载**：此前顶层静态 import 致主进程启动即载（实测 heap +9.7MB / RSS +24MB），改为与 sharp 同款首次使用加载（xlsx 导入/导出为低频操作）
- **V8 参数修正**：删 `--max-semi-space-size=16`（Node 20 / V8 11.3 起 semi-space 随堆上限自适应缩小，显式 16MB 反把每个 isolate 年轻代抬到 ~48MB，nodejs/node#55487）；堆上限 1024 → 768（PDF 内嵌有 100MB 文件上限兜底）
- **工作区索引配额**：目录快照 LRU 4096 → 512（单工作区工具，理论上限 60-120MB → ~8-15MB）
- **渲染层**：`v8CacheOptions: 'code'`（启动/重建窗口少重复编译）
- **内存实测**（Deepin，smaps_rollup 口径同 v2.4.5）：空载醒着 487 → **464MB**（主 239 / 渲染 153 / utility 70）；托盘常驻 310 → **295MB**（主 224 / utility 70，渲染 30s 归零；PSS 178 / 独占 126 / swap 0）
- 如实记录：`--in-process-gpu` 曾按调研结论当冗余删除，实测 Deepin 下 `--disable-gpu` 仍创建 GPU 进程（~149MB RSS），删除后托盘合计反升 +50MB，已恢复并以实测为准

### 性能优化

- **图片预览降采样管线**：预览弹窗不再原图全尺寸解码（6000×4000 ≈ 96MB 位图），主进程 sharp 生成 2048px 副本（解码 ≤16MB），与缩略图同根缓存 + mtime 失效 + `qihebox://thumb/` 长缓存；失败自动回退原图，功能不受影响
- **搜索结果虚拟化**：Search 页结果网格换 VirtualGrid，几千命中不再 DOM 爆炸
- **图片缩略图 IPC 并发闸**：上限 8 + FIFO 排队 + 卸载跳过，快速滚动大列表不再 IPC 洪峰
- **Images 页排序**：path→时间戳 Map 一次计算、全页共享一次排序结果，筛选/搜索不再每对元素 `new Date()`（万级条目每轮省 10 万+ 次解析）
- **回收站缩略图类型修正**：视频/zip/文本不再误判为 image 空打缩略图 IPC

### 附带修复（v2.4.4 潜伏 bug，代码实证）

- **视频抓帧闸永久卡死**：排队请求被无条件按「已取消」唤醒、槽位双重自增只释放一次——≥3 个视频并发两轮后抓帧全局卡死（`FileThumbnail.tsx` pumpFrameFetch/awaitFrameSlot）
- **视频帧缓存脱绑调用**：`const fn = this.thumbs.videoThumbnail; fn()` 丢失 this，缓存读/写自 v2.4.4 起一直静默失败，每次访问都重抓帧（`core/index.ts` saveVideoFrame/videoThumbnail）

### 测试

- **142 单测 + 21 e2e 全绿**（e2e 新增 previewUrl 管线断言）；双 tsconfig 零错误；双平台打包见 `软件发布包/` 与 release/

## v2.4.5 — 2026-08-11（运行时降版 + 内存瘦身，无新功能）

### 运行时降版（Electron 43 → 31.7.7）

- **Electron 43 → 31.7.7**（Chromium 126 / Node 20.14，sharp Node≥20.9 硬地板之上；electron-vite 5 / electron-builder 26 / Playwright 1.62 工具链全兼容）
- **pdfjs-dist 5.6 → 4.8.69**：5.6 依赖 `Promise.try`（Chrome 128+），Chromium 126 不存在致 PDF 预览白屏；4.9 起引入该调用，回退后渲染层 API 零改动、PDF e2e 转绿
- **托盘休眠提速**：关窗到托盘后销毁窗口倒计时 2 分钟 → **30 秒**（30s 内回切零成本；超期重建由 v2.4.3 唤醒修复保障）
- **内存实测**（Deepin，smaps_rollup 进程树口径，`scripts/measure-memory.mjs` 可复测）：空载 580 → **487MB**（-16%，主 260 / 渲染 155 / utility 71）；托盘常驻 408 → **310MB**（-24%，渲染 30s 后归零）
- **包体积**：AppImage 97MB / deb 98MB / Windows Setup 96MB（红线 140/195，较 E43 进一步缩小）
- **NetworkServiceInProcess 砍 utility 进程**：实测 Electron 31 不尊重该 feature（utility 仍独立），按预案撤销未固化，如实记录
- 测试：**141 单测 + 21 e2e 全绿**（E31 下实测）；遗留：Windows 对照数与万图场景抽查待实机/CI

### 附带修复（e2e 基础设施）

- **e2e 与生产应用 userData 隔离**：`QIHEBOX_E2E=1` 时 userData 指向独立临时目录——此前共享 `~/.config/启禾文件管理`，生产应用在跑时单实例锁把 e2e 实例判为二次启动、启动即退（本机 e2e 全灭的真正根因）；同时杜绝 e2e 读写真实缩略图/索引/账号文件。修复后 e2e 21/21 全绿

## v2.4.4 — 2026-08-11（压缩分享 + 解压 + 视频浏览 + 标签体系完善）

### 临时压缩分享

- **多选 → 右键「压缩分享」**（证书库/图包库/文件浏览器，含选择栏按钮）：流式压缩到 `工作区/导出/`，自动命名（全部源同属一个产品集用 `产品集名_分享`，否则 `分享_时间戳`），冲突自动加序号
- **进度弹窗**：实时进度 + 当前条目 + 可取消（取消自动清理半成品包）；完成后「打开所在文件夹 / 复制到剪贴板」直接外发微信/钉钉
- 零新增依赖：手写 ZIP 容器 + node 内置 zlib，全程流式恒定内存（与文件大小无关）；压缩/解压共享单任务锁；单包限 4GB/65535 条目（ZIP64 未引入，超限明确报错）

### 解压

- 右键 `.zip` → 「解压到当前文件夹 / 解压到 &lt;包名&gt;/」，进度可取消
- **zip-slip 防护**：拒绝绝对路径 / `..` 逃逸 / 盘符 / 符号链接条目（单测覆盖）；逐条目 CRC 校验，损坏文件自动清理
- **GBK 文件名解码**（Windows 压缩包常见）；同名冲突自动加 `_1` 序号不覆盖
- 仅支持 .zip（rar/7z 不在范围）

### 视频浏览

- **视频帧缩略图**：渲染层 `<video>`+canvas 抓帧（不引 ffmpeg），落 userData 与图片缩略图同根同哈希，二次秒显；并发 ≤2、15s 超时、不可解码格式（mkv/avi）自动回落 🎬 不报错
- **图包库类型筛选**：图片 / 视频切换聚合浏览（`fileList` 新增 `media_type` 过滤参数，`file_type` 定目录、`media_type` 定筛选）

### 标签体系完善

- **搜索命中标签**：全局搜索关键词可命中产品集与文件标签（产品集结果不再硬编码空 tags）
- **文件卡片标签展示**：FileEntry 带 tags（list 时从元数据缓存 join，纯内存），浏览器/图包库/证书库/搜索结果卡片显示标签 chips（≤2 +N）
- **标签筛选铺开**：文件浏览器/图包库/证书库新增标签筛选，与产品集/子文件夹/类型筛选组合
- **批量打标**：多选 → 右键「打标」/ 三页选择栏按钮 → 弹窗添加/移除（即时生效、一次落盘）；证书状态标记的主入口
- **打标组件增强**：回车命中未定义名 → 「新建标签」即建即用（不再制造孤儿）；「父/子」语法直接建子标签；候选下拉键盘上下键 + Enter 导航
- **设置页补齐**：顶层标签「移至父级」；alert/confirm 统一 toast + 确认弹窗；删除提示影响范围（从 N 处移除）
- **引用源注册机制**（core 重构）：标签 rename/delete/adopt/迁移/计数统一遍历注册的引用源（文件 + 产品集内置），v2.6 客户 / v2.7 发票接入只需 `registerSource`，不再手写逐库传播

### 测试与验收

- 新增 archive 单测 9 例（往返/解压模式/zip-slip/GBK/冲突/取消/CRC 篡改/自动命名/单任务锁）、files media_type 1 例、标签引用源 1 例、搜索命中标签 2 例、批量打标 1 例；单测 **141 例全绿**，TSC 零错误，构建通过
- e2e 当时在本机启动失败的根因已在 v2.4.5 查明并修复（e2e 与生产应用共享 userData 触发单实例锁，见上），现 21/21 全绿

## v2.4.3 — 2026-08-10（休眠唤醒修复 + 保存反馈）

### 休眠后黑屏、无法唤醒（四个缺陷叠加修复）

- **唤醒不再提前 show**：`windowShow()` 重建窗口时不再立刻显示，改由 `ready-to-show` 渲染就绪后才显示（此前重建后先露出深蓝近黑底色空窗，加载慢/卡住即"黑屏"）
- **加载底色改浅**：`#0f172a` → `#f8fafc`（与界面一致），加载期观感从"黑屏"变"加载中"
- **唤醒活性检查（唤醒机制本体）**：每次唤醒（托盘点击/恢复）ping 渲染进程（2s 超时），假死但未崩溃（不触发 render-process-gone）时自动 reload，解决"黑屏后没有任何办法恢复"
- **最小化休眠恢复自愈**：最小化 2 分钟不可见 reload 后置标记，恢复时在可见状态再校验一次活性
- **GPU 进程崩溃自动恢复**：崩溃后 1 秒 reload 主窗口一次，纳入崩溃时间窗计数（10 分钟 ≥3 次才退出，防循环）
- **诊断可查**：休眠/唤醒路径补 `[wake]`/`[sleep]` 日志；休眠定时器支持环境变量覆盖（`QIHEBOX_DESTROY_DELAY_MS` / `QIHEBOX_MINIMIZE_RECOVER_MS`），可用 10 秒完整跑休眠→唤醒循环自查

### 右键「编辑信息」→「保存元数据」无反馈

- **通用 toast**：`showToast`（成功绿 / 失败红，默认 3s 自动消失），证书到期横幅行为不变（红色 15s）
- **保存全链路反馈**：保存成功弹「元数据已保存」，失败弹原因；保存中按钮防连点（显示"保存中..."）

### 交互体验

- **仪表盘可点击**：「最近文件」点击直接打开预览；统计卡片（产品集/图片/证书）点击跳转对应页面；「到期提醒」点击跳到证书库并自动按产品集过滤

### 测试

- 新增 notifyBanner 单测 4 条；单测 127 + e2e 21 全绿

## v2.4.2 — 2026-08-09（上线前审计修复 + 性能与稳定性）

> AI 功能（v2.2.0 实现）仍不开放：客户端入口隐藏（`FEATURE_AI=false`），代码保留在独立 `feature/ai` 分支。

### 核心修复：切换文件夹图片渲染不出来

- **VirtualGrid 响应式化**：网格行数 `count` 改为 getter、行内容改为响应式派生（旧实现一次性求值被冻结 + 行死切片，切文件夹后网格停留在旧内容/新旧混杂）
- **缩略图队列重构**：新增 `ThumbQueue`——浏览请求插队优先、切文件夹时作废旧积压（代际作废）、单任务 15s 超时防死锁、队列上限 200；导入/改名等后台任务不再饿死浏览
- **缩略图容错**：URL 经 `img.onload` 验证后才进渲染层 LRU 缓存，加载失败自动剔除并重试（坏图不再永久空白）
- **协议缓存头**：缩略图响应 `Cache-Control: immutable`，回切文件夹秒显

### 交互体验（v2.4.1 的 250ms 延迟判定废弃）

- **单击选中 / 再点取消 / 双击打开**：文件浏览器、图包库、证书库统一——单击立即切换选中（无延迟），双击打开预览
- **右键即时可用**：右键未选中项自动先选中再出菜单；修复「菜单刚打开就被自动关闭」的根因（事件冒泡到 window 关闭监听器）
- **证书库到期徽标**：证书卡片显示到期日 + 倒计时（30 天内红色）

### PDF / 媒体

- **修复 dev 下 PDF 打不开**（vite 预打包给 worker 注入 `/@vite/client` 导致 blob worker 加载失败）：排除 `pdfjs-dist` 预打包 + dev 用 `?raw` 原始源码
- **视频可拖进度条**：协议层手写 Range/206（旧实现 `net.fetch` 透传不回 206）
- **超大 PDF 保护**：>100MB 引导「用系统程序打开」（整载有 OOM 风险）
- **预览防泄漏**：pdfjs 加载中关闭弹窗不再泄漏 document + worker；连开多个文件时慢返回不再覆盖当前预览

### 数据安全与健壮性

- **元数据 key 重构**：`产品集/图包|证书/子文件夹/文件名`（跨平台分隔符统一，旧数据懒迁移）——不同子文件夹同名文件不再串号/误删
- **批量操作部分失败聚合**：删除/移动/导入返回成功与失败明细，单个失败不中断整体，重试不产生重复副本
- **路径安全**：名称入参防路径穿越、saveTextFile 白名单、PowerShell 注入修复、协议 realpath 防符号链接逃逸
- **导入链路**：互斥锁防并发同名覆盖、元数据批量落盘（消除 O(n²) 全量重写）、缩略图异步生成不阻塞导入
- **崩溃自愈**：崩溃计数改时间窗（10 分钟 3 次才退出），杜绝「崩溃→清零→再崩溃」无限循环
- **证书提醒**：到期日期宽松解析 + 写入归一化、已删除证书不再提醒、通知不可用时降级为应用内横幅
- **内存**：metadata 缓存按 mtime 失效（双机同步覆盖不再丢）、日志启动清理 14 天前、NSIS 卸载策略落档（保留用户数据）

### 剪贴板

- **Windows 复制文件**：改 PowerShell `Set-Clipboard`（旧 CF_HDROP 写错格式 ID，粘贴无内容）——需 Windows 实机验证
- **Linux 复制**：xclip 挂起修复、无 xclip 时明确报错不假成功

### 测试

- 单测 123 例全绿（+13：缩略图队列 5、命名无扩展名冲突、导入容错、元数据隔离/懒迁移、证书日期与存在性）
- e2e 21 例全绿（+2：PDF 预览回归、切文件夹强断言——src 集合无交集 + 数量吻合）

## v2.4.1 — 2026-08-09（Everything 式索引 + 交互体验）

> AI 功能（v2.2.0 实现）仍不开放：客户端入口隐藏（`FEATURE_AI=false`），代码保留在独立 `feature/ai` 分支。

### 性能：Everything 式精简索引

- **全量索引快照**：打开工作区一次构建全工作区文件索引（内存 + 落盘 `userData/index/<workspaceHash>/index.json`），二次启动加载免全扫
- **切换文件夹零磁盘 IO**：查询纯内存命中（仅 1 次目录 stat 签名比对），万级文件目录瞬时切换
- **变更驱动**：只有目录真实改动才重建该目录——应用内写操作显式失效（dirty 标记），外部变更由 `fs.watch` 文件系统监听即时感知（inotify 不可用时自动降级签名校验）
- **内存小**：快照存紧凑元组条目（`[name,size,mtime,type,thumb]`），path 与缩略图路径查询时纯函数推导——万级文件约 1-2MB，LRU 4096 目录兜底

### 交互体验

- **单击选择 / 双击打开**：文件浏览器、图包库、证书库、搜索的文件卡片——单击切换选中（250ms 延迟判定，双击自动清除），双击打开预览；搜索文件项右键联动选中集
- **右键菜单关闭优化**：任意鼠标按键点击外部、再次右键、滚动列表均立即关闭（此前不随点击消失）；菜单内点击不误关

### 标签

- **颜色克制化**：标签 chip 改中性深灰文字 + 颜色 8% 透明度淡底 + 细边框 + 实色圆点，整体素雅
- **子父标签选择**：打标下拉展开子标签（缩进 + `父/子` 前缀 + 色点），子标签可直接点选（此前候选仅顶层标签）

### 其它

- 文件列表缓存全面替换为索引（移除旧 FileListCache）
- 测试 110 用例全绿（+5：索引 10 例、文件索引适配）

## v2.4.0 — 2026-08-09（CI 门禁 + 应用内更新 + 打磨与体验）

> AI 功能（v2.2.0 实现）仍不开放：客户端入口隐藏（`FEATURE_AI=false`），代码保留在独立 `feature/ai` 分支。

### CI 门禁（GitHub Actions）

- 新增 `.github/workflows/ci.yml`：push 全分支 + PR 到 master 触发；bench 不进 CI（避免改写 docs/PERF.md 污染基准）
- **test**：tsc 双配置类型检查（renderer / main+preload+tests）+ vitest 单测 + electron-vite build
- **e2e**：Xvfb 无头环境跑 Playwright（19 用例），报告上传 artifact
- **package**：Linux（AppImage + deb）/ Windows（NSIS）双平台矩阵打包验证，`--publish never` 不自动发布

### 应用内更新检查

- 新增 `updater.ts`：拉取官网 `version.json` + 语义化版本比对，启动时 + 每 24h 静默检查
- 发现新版本经 IPC 事件通知渲染层（「我的 → 更新」显示「发现新版本 + 前往官网下载」）
- download/apply 仍未开放：更新统一引导前往官网下载全新安装包

### 标签体系重构

- 移除固定预设标签（重要/待更新/已更新/问题/归档），升级时一次性迁移清空
- 标签删除不再复活；颜色可自由修改
- tagChipStyle 亮度自适应：浅底深字 / 深底白字

### 拖拽修复

- 应用内拖出后拖回窗口不再误触发导入（内部拖拽标记 + 路径匹配）

### 右键菜单统一

- 共享 builder 统一 6 处入口（文件浏览器/图包库/证书库/搜索/产品集/预览弹窗），补齐「编辑信息」
- 新组件：TagInput（输入下拉搜索）、DatePicker（日历选择器，复刻 ERP 交互）、MoveDialog、BatchRenameDialog、EmptyState / Loading

### 文件移动与导入

- **文件移动**：moveFiles 全链路（跨子文件夹 / 跨产品集，元数据与缩略图跟随，同名冲突加序号）
- **目录导入**：拖入目录递归平铺导入
- **批量重命名**：多选 → 前缀 + 序号

### 其他打磨

- Ctrl+K 全局搜索、回收站 30 天自动清理
- 证书到期系统通知（每日去重）
- 外部 GUI 命令 5s 超时 + unref（修复 CI / 无桌面环境挂起）
- renderer ↔ core 类型共享（`src/shared/types.ts`），存量类型错误清零

### 测试

- 单测 96 用例（+33）、e2e 19 用例（+5）
- e2e 通过 `QIHEBOX_E2E=1` 模式运行（主进程跳过「关闭 → 隐藏托盘」，适配 CI 无桌面环境）

## v2.3.1 — 2026-08-09（回收站）

> AI 功能（v2.2.0 实现）仍不开放：客户端入口隐藏（`FEATURE_AI=false`），代码保留在独立 `feature/ai` 分支。

### 新功能：回收站

- **删除不再不可恢复**：文件、子文件夹、产品集删除后统一移入回收站（工作区 `.qihefilemanager/trash/`），侧边栏「回收站」可查看
- **恢复**：一键还原到原位置；同名冲突自动加「-恢复N」后缀；子文件夹恢复后自动加回图包/证书类型配置
- **彻底删除 / 清空**：单项彻底删除或一键清空（确认后不可恢复，此时才清理元数据与缩略图缓存）
- **删除时保留元数据与缩略图**：恢复后文件标签、备注、缩略图原样可用；产品集删除走回收站，目录移走即从列表消失，恢复后 tags/notes 保留
- 各处删除确认文案已更新（提示"将移入回收站，可恢复"）

## v2.3.0 — 2026-08-08（拖拽体验升级 + 标签体系对齐 + 分层休眠 + 内存压制）

> AI 功能（v2.2.0 实现）仍不开放：客户端入口隐藏（`FEATURE_AI=false`），代码保留在独立 `feature/ai` 分支。

### 拖拽体验升级

- **跟手 Ghost 图标**：拖出文件时拖拽图标从固定 logo 换成**该文件真实缩略图**（主进程读 userData 缩略图缓存，image/pdf 有效，其余兜底 logo）；多文件由系统显示叠影
- **导入进度 + 可取消**：拖入批量导入时底部进度条（已完成/总数 + 百分比），可随时取消，已复制的文件保留；导入完成/失败/取消均有明确提示

### 标签体系对齐（设置标签 = 实际标签）

- **根因修复**：文件/产品集标签此前允许自由文本输入 + AI 打标不校验 → 可写入「未定义标签」；设置页只管理 tags.json 定义，两边对不上
- **孤儿标签治理**：设置页新增「未定义标签」区块——被引用但无定义的标签（历史自由输入 / AI 打标引入）以灰色 chip + 引用计数展示，可**转为正式标签**（选色补定义，引用不动）或**清除引用**
- **打标入口统一**：文件预览与产品集标签输入框加自动完成（datalist，候选=已定义标签）；已打未定义标签显示黄色警告
- **AI 打标过滤**：AI 建议只应用已定义标签，未定义者忽略并在提示中注明数量
- 重命名/删除标签仍全局同步（文件 + 产品集引用），现在同样适用于孤儿标签

### 分层休眠（不用时降内存）

- **第一层 · 失焦 30 秒**：清理 Blink 图像解码缓存，回来重新解码
- **第二层 · 最小化 2 分钟**：渲染进程 reload 回收（窗口不可见，无感）
- **第三层 · 关闭留托盘 2 分钟**：销毁 BrowserWindow，内存只剩主进程 + 托盘（~30-50MB 级）；托盘点击 / 二次启动即时重建，并**恢复上次所在页面**（路由持久化 localStorage）
- 路由状态：窗口重建后自动回到上次浏览的页面

### 内存压制（官方 Performance 清单逐项落地）

- `Menu.setApplicationMenu(null)`：frameless 无原生菜单，禁用默认菜单初始化
- V8 参数收紧：`--max-old-space-size 2048 → 1024`、`--max-semi-space-size 32 → 16`（GC 更积极，常驻内存下降）
- `webPreferences.webgl: false`：应用无 WebGL 场景，省 GPU 上下文初始化
- 缩略图卸载即释放：虚拟滚动滚出视口 → `img.src=''` 释放解码位图（内存只留当前屏，URL 缓存保留免重复 IPC）
- 实测数据：见 docs/PERF.md 与下方「内存实测」节

### 内存实测（打包版，Deepin）

- 常驻 3 进程 ~580MB（Electron + Deepin 软件渲染固有基线，与 v2.2.1 持平；V8 参数实测对主进程常驻影响有限，主要收益在 GC 触发时机）
- **休眠态 -170MB**：关闭到托盘 2 分钟 → 窗口销毁、渲染进程归零，总内存 575MB → **408MB**（主 329 + utility 77，托盘常驻）；托盘点击 / 二次启动即时重建（580MB）并恢复上次页面
- 缩略图滚出视口即释放解码位图 + 失焦 30 秒清缓存：长目录滚动内存增量更小、回落后不再滞留

## v2.2.1 — 2026-08-08（修复专项：拖拽 / 子文件夹 / PDF / 内存长稳）

> AI 功能（v2.2.0 实现）本期**不开放**：客户端入口已隐藏（`FEATURE_AI=false`），代码保留在独立分支，成熟后发布。

### 修复

- **拖拽拖出失效（打包回归）**：`build/logo.png` 未进 asar 导致 startDrag 图标为空，安装版无法拖出；已加入打包白名单
- **图包/证书子文件夹不可修改**：新增「重命名」能力（设置页 ✎ 按钮），改名会**同步迁移所有已有产品集**下的同名目录（幂等：未建目录的产品集自动跳过）；metadata 按「产品集/文件名」存储，无需迁移
- **PDF 预览重做（打包版曾空白/报错/闪退/极慢）**：
  - **改用 pdfjs-dist 官方 `PDFViewer` 组件**（Mozilla PDF.js 官方查看器核心，不再自写渲染逻辑）
  - **连续滚动 + 渲染队列**（只渲染视口页 + 空闲预渲染，翻页/滚动流畅，解决延迟）· **文本层**（文字可选中复制）· **全文搜索**（高亮 + 上下匹配跳转）· 缩放（适配页面/40%-400%）· 页码跳转
  - 流式加载（Range 请求，不全量读内存）+ worker 单例 + 组件销毁清理
  - 实测：16 页质检报告完整渲染 + 文本层激活（e2e 验证）
  - 集成修复：官方组件需先注入全局 `pdfjsLib` 再动态 import；容器恒渲染保证 ref 绑定
- **图片预览**：全分辨率大图，主窗口内嵌查看

### 内存长稳（常驻进程最少化）

- 修复 4 个真实增长点：metadata 缓存 LRU（3 工作区）、CountCache LRU（2048）、PdfPreview blob 单例、sharp/libvips 缓存收紧
- V8 参数（半空间调小，长期运行更快归还内存）+ `--in-process-gpu` 去 GPU 空壳进程（Linux 打包实测验证）
- **MemoryWatchdog**：5 分钟基线 + 60 秒采样，渲染进程超阈值自动优雅 reload；24 小时定时 reload 保底；窗口隐藏时清理 Blink 图像缓存；崩溃自愈后重置计数
- 目标：常驻内存稳定（去 GPU + 懒加载实测），长期运行不增长；PDF 流式加载避免大文件全量占内存

### 服务端配套（ERP 平台管理员）

- 平台管理员新增「文件管理」tab：box 用户列表（设备/活跃/AI 用量）、概览统计、版本/平台分布、7 天活跃趋势、服务端组件可用性

## v2.2.0 — 2026-08-08（账号登录 + AI 智能整理 + 活跃统计）

### 新功能：可选登录（复用「启禾 OS」账号）

- 「我的」→「账号」：邮箱+密码登录，不登录完全不影响本地功能
- 登录后自动上报活跃（设备标识+平台+版本+最近使用），用于统计活跃用户；可随时登出停止
- token 优先系统安全存储加密，Linux 无 keyring 时降级明文（本地单用户，JWT 过期即失效）

### 新功能：AI 智能整理（登录解锁，新用户 50 次试用）

- **AI 批量命名**：多选文件 → 右键「AI 命名」→ 按命名模板批量生成规范名 → 预览勾选后应用
- **AI 标签建议**：多选文件 → 右键「AI 打标」→ 只从现有标签中推荐 → 一键批量打标
- **证书信息抽取**：PDF 预览 →「AI 抽取信息」→ 自动填证书类型/到期日/编号/发证机构
- **AI 语义搜索**：搜索页自然语言查询（如「上周的营业执照」）→ AI 翻译关键词+条件 → 本地执行
- 额度：每账号累计 50 次 + 每日 30 次上限（服务端校验，防滥用/控成本）；用完仅 AI 断供，本地功能不受影响

### 隐私边界

- AI 仅上传文件名 / PDF 纯文本 / 命名模板 / 现有标签；图片与文件本体、目录结构永不上传
- 服务端处理完即丢，不留存请求内容；PRIVACY.md 已更新

### 服务端（ERP 后端配套）

- 新增 `box_devices`（活跃心跳 upsert）与 `box_ai_quota`（试用额度）集合
- 新增 `POST /api/box/heartbeat`、`POST /api/box/ai`（4 个 action），复用全局 AI 配置链与结构化输出模式（Generate→JSON）

## v2.1.2 — 2026-08-08（包瘦身）

- **安装包 -10%**：pdfjs-dist（101MB）实际进包的 39MB 全部排除——渲染层已把它打进 bundle（worker 复制到 assets），node_modules 里是死重。asar 68MB → 29MB（-57%），deb/AppImage 137MB → **124MB**，Windows zip 同步减小
- 验证：安装版冒烟全绿（PDF 预览 bundle 加载正常、缩略图 36/36、4 进程）

## v2.1.1 — 2026-08-08（进程瘦身）

### 进程合并（7 → 4，总内存 ~800MB → ~600MB）

- **去掉 3 个 zygote 孵化器**（`--no-zygote`，-118MB）：Chromium 直接 spawn 子进程；参数固化为启动参数（`linux.executableArgs`），Linux 生效
- **禁用 GPU 进程**（`--disable-gpu`，207MB → 90MB 空壳）：Deepin 本就软件渲染，纯开销；无崩溃
- **实测证伪**：`--single-process` 极限合并（1 进程）在 Deepin 上启动即崩（SIGTRAP），已放弃；`--js-flags` / `disableHardwareAcceleration` 历史崩溃坑，不启用
- Windows 不受影响（无 zygote，保留 GPU 加速）

### 实测（Deepin 安装版）

- 进程：7 → 4（主 280 / 渲染 148 / GPU 90 / utility 77）
- 总内存：~800MB → **~600MB**；万图滚动渲染进程增量 ≤41MB 不变

## v2.1.0 — 2026-08-08（内存性能专项 + PDF 预览）

### 修复（真实环境验证）

- **闪退（SEGV）**：真实证书 PDF（含 Form XObject/嵌入字体）在 `pdfjs + @napi-rs/canvas` 渲染时会原生段错误 → 主进程崩溃闪退。**不再用该路径渲染 PDF**
- **PDF 预览不渲染**：Chromium PDFium 不渲染 iframe 中的 `qihebox://` 自定义协议 PDF，改为 pdfjs-dist 渲染进程 canvas 渲染（canvas + 上一页/下一页/缩放，worker 走 blob URL 兼容打包环境）
- 验证：真实工作区 97 个证书 PDF 抽样——预览 17 页报告可翻页、应用不闪退

### 产品决策：PDF 不做缩略图

- 曾尝试隐藏窗口 PDFium 渲染 PDF 首屏缩略图，实测成本高（每张 ~2s 窗口渲染、批量排队）且失败回退 📄 等于白做——**取消**。缩略图仅图片（sharp）；PDF 证书在列表中保持 📄 占位，点开用 pdfjs 预览查看内容

### 内存性能（万图场景内存不再失控）

- **虚拟滚动**（`@tanstack/solid-virtual`）：图包库/证书库/产品集文件页只渲染视口 ± 缓冲行，滚出即卸载。实测 1 万张图目录 DOM 恒定 ~40 个卡片（旧版滚到底挂 1 万个节点）；渲染进程内存增量 ≤41MB 且回顶回落
- **缩略图缓存迁移到 userData**：缩略图不再写入工作区 `.thumbnails`（坚果云目录不被缓存文件刷屏）；旧缓存自动迁移复用；新协议 `qihebox://thumb/` 独立提供
- **缩略图 URL 渲染层 LRU 缓存**：虚拟滚动回滚时命中率≈100%，免重复 IPC 往返
- **关闭拼写检查**（`spellcheck: false`），省渲染进程资源

### PDF 预览

- 证书 PDF 在列表保持 📄 占位，点开后用 pdfjs 渲染进程渲染（canvas，上一页/下一页/缩放）——解决原 iframe 预览不显示的问题

### 实测（Deepin，1 万张图）

- DOM 节点 36~54 恒定；渲染进程 156MB（空载）→ 197MB（滚动峰值）→ 回落 194MB
- 总内存基线 ~700MB 为 Electron 43 + 软件渲染 GPU 的固有开销

## v2.0.2 — 2026-08-08（标签层级 + 性能 + 页面）

### 标签体系升级

- **父/子标签层级**：标签可设父级（两层结构）；管理页树形展示（子标签缩进）；打标签时按父/子分组快速选择；chip 显示层级（`父/子`）；删除父标签时子标签自动提升为顶层
- **固定色预设标签**：内置 5 个（重要红/待更新橙/已更新绿/问题黄/归档灰），颜色固定、可重命名/删除；旧工作区自动补全

### 性能优化

- **路由级懒加载**：首屏只加载仪表盘，其余页面按需分包（减小首屏 bundle）
- **大列表分批渲染**：图包库/证书库万级文件只渲染可见区，滚动加载更多（IntersectionObserver）
- **exceljs 延迟加载**：主进程启动不再加载其依赖链

### 页面

- **帮助页**挂载 `/help` 路由 + 侧边栏入口（此前为死页面）
- **我的 → 更新**：最新/失败状态显示当前版本 + 「前往官网下载」引导

### 修复

- 拖拽拖出到微信变路径：`preventDefault` 阻止 HTML5 默认拖拽，让 `startDrag` 原生文件拖拽接管（需用户实测确认）

## v2.0.1 — 2026-08-08（修复 + 升级）

### 缩略图修复（关键）

- **根因**：旧版用绝对路径 sha256 作缩略图缓存键，Windows 生成的缩略图在 Linux 下路径不同导致全部失效
- **修复**：缓存键改为「相对工作区路径」hash（跨平台一致，坚果云 Win/Linux 双机共享也可复用）；新增 `ensureThumbnail` IPC，缩略图缺失自动生成
- 验证：真实工作区「团队共享」旧图缩略图自动补齐显示

### 新功能

- **标签体系升级**：全局标签定义（`tags.json` 颜色体系，自动迁移旧版 `tags_state.json`）；Settings 新增标签管理（新建/改色/重命名/删除，重命名与删除同步所有文件与产品集引用）；文件预览与产品集标签按颜色显示
- **拖拽拖出**：从图包/证书库直接拖文件到桌面/文件管理器/微信（Electron `startDrag` 原生多文件拖拽）
- **右键体系**：统一 ContextMenu 组件（消除 4 页面重复代码），补全操作：用默认程序打开 / 复制路径；多选支持复制/文件夹显示/删除
- **图标**：Linux 任务栏/窗口图标（BrowserWindow icon）

### 修复

- `shell.openPath` 在 Linux 挂起导致"用默认程序打开"IPC 永不回复 → 改用 `xdg-open` 子进程
- preload 返回值与 Wails 语义对齐（getSize/getPosition/isMaximised 解包）

### 测试

- 单测 28（新增标签体系 4 用例）；e2e 15（新增标签/拖拽/复制路径/右键 4 用例）

## v2.0.0 — 2026-08-08（Electron 重构版）

### 技术栈迁移：Wails v2 + Go → Electron + TypeScript

- **主进程**：TypeScript 全栈（原 Go 2866 行 → TS），业务层 `src/main/core/` 与 Electron 解耦，可在 node 环境直接单测
- **前端**：SolidJS + Tailwind 原样保留（4595 行零重写），仅替换 Wails 绑定层为 preload IPC（`window.qihebox`）
- **平台支持**：Windows + Linux（Deepin 实测）。自带 Chromium 绕开系统 WebKitGTK 依赖，修复原版 Linux 剪贴板/资源管理器空实现问题
- **数据兼容**：工作区格式（config/metadata/缩略图哈希路径）与 v1.x 完全一致，旧工作区零迁移

### 性能（对比原版优化）

- 目录扫描缓存（目录树 mtime 签名）：Dashboard 首扫 2000 文件 **130ms**（原同步递归）
- 搜索异步 + 防抖 + 缓存：**138ms / 2000 文件**
- 导入 100 文件（含 sharp 缩略图）：**293ms** 不阻塞 UI
- 大图/PDF/视频预览走 `qihebox://` 自定义协议（流式 + Range），不再整读内存

### 稳定性

- 渲染进程崩溃自动 reload（限 3 次）；GPU 崩溃降级记录
- JSON 原子写（tmp+rename）；`metadata.json` 损坏自动备份 `.corrupt-<ts>` 并降级
- 主进程日志落盘（`app.getPath('logs')` 按日期轮转）
- 单实例锁（替代原 CreateMutex）；关闭窗口=隐藏托盘

### 平台能力

- 剪贴板复制文件：Windows CF_HDROP / Linux text/uri-list（Deepin X11 实测）
- 资源管理器选中：dde-file-manager（Deepin）/ nautilus / dolphin / shell
- 打包：AppImage + deb（Linux）/ NSIS（Windows）；体积 104MB（asar+maximum 压缩+语言包精简）

### 测试体系（原 Go 测试迁移 + 新增）

- vitest 单测 24 用例（命名引擎/路径校验/工作区/元数据/XLSX）
- Playwright e2e 11 用例（窗口/IPC/缩略图/协议/剪贴板/资源管理器/窗口控制/XLSX）
- 性能基准 `npm run bench` → docs/PERF.md

## v1.3.0 — 2026-08-05（开源版）

### 开源免费化

- **转为开源免费软件**：移除全部授权逻辑（激活 / 设备绑定 / 远程状态校验），`internal/license/license.go::Check()` 恒返回已激活（`OpenEdition` 开关，前端零改动）
- **剥离敏感与商业资产**：代码签名证书私钥（`certs/`）、授权服务器（`license-server/`）自仓库移除，`git` 历史重建为干净起点
- 无需 License Key，下载即用全部功能

## v1.2.5 — 2026-06-26

### 代码签名

- **自签名代码签名**：exe 和 installer 现在使用自签名证书签名，属性显示发布者「启禾软件」（不再是"未知发布者"）
- 新增 `scripts/generate-cert.sh`：一键生成自签名代码签名证书（RSA 4096，10 年有效）
- `build-server.sh` 构建后自动调用 `osslsigncode` 签名 exe + installer
- 用户安装 `code-signing.crt` 到受信任根后可消除本地 SmartScreen 警告

### 版本号

- 安装包文件名包含版本号：`qihefilemanager-1.2.5-amd64-installer.exe`
- 同时保留固定名 `qihefilemanager-amd64-installer.exe` 作为稳定下载链接

### 其他

- 更新检查 URL 从 `/box/version.json` 改为 `/version.json`（官网合并进 ERP SPA 后）
- `version.json` 产物路径改为 `web/public/version.json`

---

## v1.0.0 — 2026-06-25

第一个正式可用版本，面向电商运营与销售团队的本地化产品图包 & 证书资料管理工具。

### 核心功能

- **工作区管理**：自包含工作区，文件、配置、元数据、缩略图全部在一个目录内，复制即迁移。
- **产品集管理**：
  - 创建、删除、空产品集重命名。
  - 产品集标签与备注，支持按名称/标签筛选。
  - XLSX 批量导入创建产品集。
- **图包 / 证书分类**：
  - 自定义子文件夹（图包：主图、详情页、白底图、素材；证书：3C、质检、专利等）。
  - 拖拽导入，自动按命名模板重命名。
  - 导入时自动生成 256×256 缩略图。
- **文件外发**：
  - 复制到系统剪贴板（`CF_HDROP`），支持微信/钉钉 `Ctrl+V` 粘贴发送原图。
  - 在资源管理器中打开并高亮选中文件，方便拖拽到聊天窗口。
- **浏览与检索**：
  - 全局搜索产品集与文件名。
  - 图包库 / 证书库支持按产品集、子文件夹筛选，按时间/名称/大小排序，文件名搜索。
  - 多选批量复制、删除、在文件夹中显示。
- **预览与元数据**：
  - 图片、PDF、视频预览。
  - 文件级元数据：标签、备注、证书类型、到期日。
  - 用系统默认程序打开文件。
- **系统托盘**：最小化到托盘，常驻后台。

### 构建产物

- `build/bin/qihefilemanager.exe`（约 14 MB，便携版）
- `build/bin/qihefilemanager-amd64-installer.exe`（约 7 MB，NSIS 安装包，支持卸载）

---

## 2026-06-24

### 新增

- **导出文件桥接方案（替代原生 Drag-Out）**
  - 由于 WebView2 自身会接管拖拽并携带预览位图，无法被 Go 侧替换为 `CF_HDROP`，原生拖拽导出不可靠，改为桥接方案。
  - 后端新增 Windows-only `internal/clipboard` 包：通过 `CF_HDROP` 把文件复制到系统剪贴板，支持在微信、钉钉等聊天窗口按 `Ctrl+V` 批量粘贴发送原图。
  - 后端新增 Windows-only `internal/explorer` 包：通过 `SHParseDisplayName` + `SHOpenFolderAndSelectItems` 打开资源管理器并高亮选中文件，用户可继续从资源管理器拖到聊天窗口。
  - 后端新增 `CopyFilesToClipboard` / `ShowFilesInExplorer` 接口，移除原 `DragOutFiles`。
  - 前端文件浏览器（`FileBrowser.tsx`）移除旧拖拽，新增工具栏「复制选中」与「在文件夹中显示」按钮，支持 `Ctrl+C` 批量复制已选文件。
  - 前端图包库（`Images.tsx`）移除旧拖拽，图片右键菜单支持「复制」与「在文件夹中显示」。
  - 非 Windows 平台提供空实现，前端可正常编译，运行时导出功能不可用。

## 2026-06-24

### 修复

- **文件预览**
  - 修复导入图片后无预览的问题：改为通过 Wails AssetServer 自定义 Handler 直接提供文件 URL，避免 base64 data URL 在 WebView2 中无法加载大图。
  - 导入完成后立即生成缩略图，文件卡片显示真实缩略图。
  - 预览弹窗支持图片、PDF 和视频，增加错误提示，加载失败时显示具体原因。
  - 预览弹窗新增「用系统程序打开」按钮，作为所有文件类型的兜底方案。
  - 后端新增 `GetWorkspaceFileUrl` 接口，返回可在前端直接使用的文件 URL。
  - 后端新增 `OpenFileWithDefaultApp` 接口，调用系统默认程序打开文件。

- **拖拽导入文件不生效**
  - 在 `main.go` 中添加 Wails v2 拖拽配置 `DragAndDrop: &options.DragAndDrop{EnableFileDrop: true}`，启用原生文件拖拽。
  - 修复 `frontend/src/components/GlobalDropOverlay.tsx` 中 `OnFileDrop` 的 `useDropTarget` 参数误用：由 `true` 改为 `false`，避免在没有 `--wails-drop-target` 样式元素时无法触发回调。
  - 优化拖拽覆盖层状态管理：使用 `onMount` 单次注册监听，增加 `drop` 事件兜底和 5 秒超时自动隐藏，防止覆盖层卡住。
  - 补回 `GlobalDropOverlay.tsx` 中缺失的 `For` 组件导入。

## 2026-06-24

### 新增

- **删除功能**
  - 后端新增 `ProductSetDelete` 接口，删除产品集及其下所有 SKU、文件，并清理相关 metadata 与缩略图。
  - 后端新增 `SkuDelete` 接口，删除 SKU 及其下所有文件，并清理相关 metadata 与缩略图。
  - 后端新增 `DeleteSubfolder` 接口，删除 SKU 下的图包/证书子文件夹，并清理该子文件夹内文件的 metadata 与缩略图，同时从工作区配置中移除该子文件夹类型。
  - 前端产品集卡片与产品集详情页新增「删除产品集」按钮，带二次确认。
  - 前端 SKU 卡片新增「删除」按钮，带二次确认。
  - 前端文件浏览器新增文件多选、全选与批量删除功能。
  - 前端文件浏览器新增「删除当前图包/证书类型」按钮。
  - 新增 `delete_test.go`，覆盖产品集、SKU、子文件夹删除及 metadata 清理。

## 2026-06-24

### 新增

- **产品集批量创建**
  - 后端新增 `ProductSetCreateWithSkus` 接口，可一次性创建产品集及其下的多个 SKU。
  - 前端"新建产品集"弹窗支持添加 SKU 列表，填写后一并提交创建。

- **XLSX 模板导入/导出**
  - 后端新增 `ExportXlsxTemplate` / `ImportProductSetsFromXlsx` 接口（`xlsx.go`）。
  - 模板包含"产品集导入模板"数据表和"填写说明"说明表，带表头样式与示例行。
  - 前端"产品集"页面新增"XLSX 模板"和"XLSX 导入"按钮。
  - 支持同一产品集连续多行，按产品集名称自动分组批量创建。

- **文件选择对话框**
  - 后端新增 `OpenFileDialog`，支持前端选择 `.xlsx` 等文件。

- **应用图标自定义**
  - 新增 `scripts/render_logo.py`，基于 `build/logo.svg` 生成：
    - `build/appicon.png`（512×512）
    - `build/trayicon.png`（64×64）
    - `build/trayicon.ico`

### 修复

- **前端弹窗溢出**
  - "新建产品集"弹窗改为响应式 2 列网格布局，模态框加宽并增加滚动，避免 SKU 输入行撑出模态框。

- **系统托盘重复创建与内存泄漏**
  - 使用 `sync.Once` 严格保证 `systray.Run` 只启动一次，防止托盘图标分裂。
  - 增加 `trayExitCh`，应用退出时正确结束托盘菜单 goroutine，避免 goroutine 泄漏。

### 文档

- 更新 `本地文件管理项目.md`，补充批量创建、XLSX 导入、图标生成等章节。
- 更新 `BUILD.md`，增加图标生成步骤与 NSIS 安装包构建说明。
- 新增 `CHANGELOG.md`（本文件）。

### 测试

- 新增 `xlsx_test.go`，覆盖 `ProductSetCreateWithSkus`、`ExportXlsxTemplate`、`ImportProductSetsFromXlsx`。
- 全部 Go 测试通过：
  ```bash
  go test -v ./...
  ```

### 构建产物

- `build/bin/qihefilemanager.exe`（约 13 MB）
- `build/bin/qihefilemanager-amd64-installer.exe`（约 7 MB）
